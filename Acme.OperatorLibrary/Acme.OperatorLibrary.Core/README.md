# Acme.OperatorLibrary.Core

Reserved for lightweight shared contracts and metadata abstractions.

Current packaging model keeps source-linked compilation in `Acme.OperatorLibrary.csproj` to avoid behavioral drift.
