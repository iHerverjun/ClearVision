# Acme.OperatorLibrary.Operators

Reserved for future split when operator implementation needs to be isolated from other runtime pieces.

Current implementation compiles linked operator sources directly in `Acme.OperatorLibrary.csproj`.
