你是一位资深软件架构师，我需要你对我的整个项目进行全面分析，提取出完整的结构化信息。
这个项目是一个类似海康威视VisionMaster的算子拖拽式低代码机器视觉开发平台。
我需要为它接入AI能力（通过自然语言自动生成工程/工作流），所以需要你帮我把项目信息提取成结构化文档。

请严格按照下面的章节顺序逐一输出，不要遗漏任何一个章节。如果某些信息在代码中找不到，请明确标注"未找到"并说明你查找的范围。

---

## 第一章：技术栈与项目架构

请回答以下问题：

1. **应用类型**：桌面应用（WPF / WinForms / Qt / Avalonia）还是 Web 应用？还是混合架构？
2. **编程语言与框架版本**：C# (.NET版本)？C++ (标准)？Python？TypeScript？其他？
3. **图形画布/节点编辑器方案**：算子拖拽连线用的什么技术实现？（WPF Canvas 自研 / 某个NodeEditor库 / AntV X6 / rete.js / 其他第三方库 / 完全自研？）请给出关键类名。
4. **图像处理后端**：OpenCV / HalconDotNet / MVSSDK / 自研算法 / 混合使用？
5. **项目结构总览**：列出所有主要模块/程序集/命名空间/文件夹，并用一句话说明每个模块的职责。格式如下：
├── ModuleA/ -- 职责说明
│ ├── SubFolder1/ -- 职责说明
│ └── SubFolder2/ -- 职责说明
├── ModuleB/ -- 职责说明

text

6. **算子注册/发现机制**：算子是硬编码注册的，还是通过反射/特性标记/插件系统动态发现的？新增一个算子需要改哪些文件？请贴出算子注册相关的核心代码片段。
7. **状态管理与执行引擎**：工作流是怎么执行的？是拓扑排序后顺序执行？还是有调度器/执行引擎？请贴出执行流程的核心代码片段或说明调用链路。

---

## 第二章：数据类型体系（端口类型系统）

这是最关键的部分之一，请务必详尽。

### 2.1 所有数据类型清单

列出项目中所有可以作为算子端口（输入/输出）传递的数据类型，格式如下：

| 类型标识（代码中的类名/枚举值/字符串） | 简称 | 说明 | 对应的实际类/结构体 |
|---|---|---|---|
| 例：ImageData | Image | 通用图像 | System.Drawing.Bitmap 或 OpenCvSharp.Mat |
| 例：RegionData | Region | 图像区域/ROI | ... |
| ... | ... | ... | ... |

### 2.2 类型继承/兼容关系

请画出类型之间的继承树或兼容矩阵。例如：
- GrayImage 是 Image 的子类型 → GrayImage输出可以连到Image输入
- int 可以隐式转换为 double
- ...

如果代码中有类型兼容性判断的逻辑，请贴出相关代码。

### 2.3 连线规则

请从代码中找出以下规则（贴出相关代码片段）：
- 一个输出端口能否连接到多个输入端口？（一对多/扇出）
- 一个输入端口能否接收多个输出端口？（多对一/扇入）
- 是否允许形成环路？
- 同一个算子的输出能否连接到自己的输入？
- 连线时的类型检查逻辑是什么？在哪个函数/方法中实现？
- 还有没有其他连线限制或特殊规则？

---

## 第三章：算子注册表（逐个算子详细描述）

这是最核心的章节，请为项目中的【每一个】算子生成以下格式的描述。
不要遗漏任何算子，哪怕是看起来不重要的工具算子。

对于每个算子，输出如下JSON结构（所有算子放在一个大数组中）：

```json
[
{
 "operator_id": "算子唯一标识（类名或注册ID）",
 "name": "算子显示名称（中文，如果代码中有的话）",
 "english_name": "英文名称（如果有）",
 "category": "所属分类（图像源/预处理/形态学/滤波/边缘检测/Blob分析/模板匹配/定位/测量/OCR/条码/颜色/标定/坐标变换/逻辑控制/数学运算/字符串处理/通信/IO/结果显示/其他）",
 "description": "详细描述这个算子做什么，2-3句话",
 "keywords": ["关键词1", "关键词2", "...用于语义检索的同义词和相关词"],
 
 "inputs": [
   {
     "port_name": "端口名称（代码中的标识）",
     "display_name": "端口显示名称（如果有）",
     "data_type": "数据类型（必须使用第二章中统一的类型标识）",
     "required": true,
     "description": "这个输入是什么、期望什么数据"
   }
 ],
 
 "outputs": [
   {
     "port_name": "端口名称",
     "display_name": "端口显示名称",
     "data_type": "数据类型",
     "description": "这个输出是什么"
   }
 ],
 
 "params": [
   {
     "param_name": "参数名",
     "display_name": "参数显示名称",
     "type": "int / double / string / bool / enum",
     "default": "默认值",
     "range": "取值范围（数值型给[min,max]，枚举型给选项列表）",
     "description": "参数说明"
   }
 ],
 
 "notes": "特殊说明（如：该算子的输入端口数量是动态的；该算子有多种工作模式，不同模式下端口不同；等等）"
}
]
特别注意事项：

数据类型命名必须和第二章中的类型清单完全一致
如果算子有动态端口（运行时可增减输入/输出），请在notes中详细说明规则
如果算子有多种模式（不同模式下端口/参数不同），请全部列出或在notes中说明
如果存在算子基类，派生出多个具体算子，请列出每一个具体算子，不要只列基类
如果算子的某些参数会影响输出的数据类型，请特别标注
第四章：工程/项目文件格式
4.1 序列化格式
工程保存为什么格式？JSON / XML / YAML / 二进制 / 自定义格式？
请贴出序列化和反序列化的核心代码（保存函数和加载函数）。

4.2 工程文件结构Schema
请根据代码分析，给出工程文件的完整结构定义。格式参考：

JSON

{
  "project_name": "工程名称",
  "version": "版本号",
  "create_time": "创建时间",
  
  "nodes": [
    {
      "node_id": "节点唯一ID（什么格式？GUID？自增int？）",
      "operator_type": "对应的算子类型标识",
      "display_name": "节点在画布上显示的名称",
      "position_x": 0,
      "position_y": 0,
      "params": {
        "参数名": "参数值"
      },
      "其他字段": "..."
    }
  ],
  
  "edges": [
    {
      "edge_id": "连线ID（如果有）",
      "source_node_id": "源节点ID",
      "source_port": "源端口名",
      "target_node_id": "目标节点ID", 
      "target_port": "目标端口名",
      "其他字段": "..."
    }
  ],
  
  "其他顶层字段": "如全局配置、画布缩放、分组信息等"
}
4.3 示例工程文件
请从项目中找出 2-3 个现有的示例工程文件，贴出完整内容。
如果项目中没有示例文件，请根据代码中的测试用例或你的理解，
手动构造 2-3 个不同复杂度的工程文件示例：

简单示例：2-3个算子的线性连接
中等示例：5-8个算子，有分支
复杂示例：10个以上算子，有分支、汇聚、循环（如果支持的话）
4.4 加载入口
当从文件/JSON字符串加载一个工程时，调用的入口函数/方法是什么？
我后续需要把AI生成的JSON喂给这个入口来渲染到画布上。
请给出完整的函数签名和所在的类/文件。

第五章：典型使用场景与工作流模式
请根据你对项目代码和算子库的理解，总结出 5-8 个最典型的使用场景。
对每个场景，提供：

text

### 场景N：[场景名称]

**自然语言描述**：（模拟用户会怎么用一句话描述这个需求）

**所需算子序列**：
算子A(参数) → 算子B(参数) → 算子C(参数) → ...

**连线关系**：
- 算子A.output_port → 算子B.input_port
- 算子B.output_port → 算子C.input_port
- ...

**关键参数配置**：
- 算子A的xxx参数建议设为yyy
- ...
请尽量覆盖不同类别：

基础图像处理流程
缺陷检测类
尺寸测量类
定位引导类
OCR/条码读取类
有逻辑分支的流程
需要通信输出的流程
其他你认为典型的场景
第六章：API/接口层（如果有）
如果项目对外暴露了API或提供了编程接口，请说明：

是否有 REST API / gRPC / WebSocket 接口？
是否有SDK/脚本接口可以通过代码创建节点、连线、执行工程？
关键接口函数列表：
创建节点：函数签名
创建连线：函数签名
删除节点/连线：函数签名
执行工程：函数签名
获取执行结果：函数签名
如果没有上述接口，从代码中找出实现以上功能的内部方法，我后续可能需要封装。
第七章：其他重要信息
请补充任何你认为对"AI自动生成工作流"这个目标有用的信息，例如：

项目中是否已有模板/预设工程的概念？
是否有算子分组/子流程/复合算子的概念？
是否有全局变量/上下文的概念？
是否有条件分支/循环/异常处理等控制流算子？
是否有撤销/重做机制？
节点在画布上的自动布局算法（如果有的话）？
任何你觉得重要但上面没问到的内容。
输出格式要求
请严格按照 第一章 → 第七章 的顺序输出
第三章（算子注册表）请输出为可直接使用的JSON数组，不要用省略号
代码片段请标注文件路径和行号（如果能确定的话）
如果内容太长需要分多次输出，请在每次结尾标注"待续"和下一部分的章节号
如果对某些信息不确定，请标注[不确定]并给出你的推测依据
