修复以下问题：PixelToWorldTransform 的射线求交没有拒绝 scale<=0 的解，还把 3D 点压扁成 2D 输出；这会在坐标系方向写反时静默给出“看起来正常”的错误世界坐标。PixelToWorldTransformOperator.cs (line 737) PixelToWorldTransformOperator.cs (line 747) PixelToWorldTransformOperator.cs (line 306)
SubpixelEdgeDetection 的传统细化并不是真正工业级亚像素模型，EdgePairDefect 也只是“全局双线间距检查”，不是局部缺陷检测。SubpixelEdgeDetectionOperator.cs (line 170) EdgePairDefectOperator.cs (line 221)
NPointCalibration 仿射路径先把点降成 float，TranslationRotationCalibration 名字写着“平移+旋转”但 angle 根本没进求解，计量语义和实现不一致。NPointCalibrationOperator.cs (line 116) TranslationRotationCalibrationOperator.cs (line 205)
高位深图像契约没守住，AdaptiveThreshold、Canny、DistanceTransform、HistogramEqualization 等路径默认按 8-bit 假设写，遇到工业常见 12/16-bit 图会失真甚至失效。AdaptiveThresholdOperator.cs (line 84) CannyEdgeOperator.cs (line 102) DistanceTransformOperator.cs (line 86) HistogramEqualizationOperator.cs (line 166)
AnomalyDetection 允许推理时覆盖训练好的 feature bank/extractor 契约，ColorDetection 缺参考色时会把 DeltaE 静默做成 0，SurfaceDefectDetection 对齐失败后继续拿未对齐参考图做差分；这三项都容易输出“看起来合理、实际错误”的结果。AnomalyDetectionOperator.cs (line 173) ColorDetectionOperator.cs (line 428) SurfaceDefectDetectionOperator.cs (line 368)
ShapeMatching 虽然已经补上金字塔，但旋转/缩放后的参考点输出仍会系统性漂移；PPFMatch 还能出现 IsMatch=true 同时 FailureReason 非空的自相矛盾结果。ShapeMatchingOperator.cs (line 172) PPFMatchOperator.cs (line 123)
ImageStitching 现在仍偏 demo，画布定位和多带融合没做完；RoiTransform 与 PlanarMatching/LocalDeformableMatching 的 pose 契约也没统一，系统级可组合性不够。ImageStitchingOperator.cs (line 165) RoiTransformOperator.cs (line 202)
FrameAveraging 的旧 Array.Sort 灾难已经修了，但中值模式仍然通过快照克隆 + 大矩阵拼接 + 全排序实现，4K 多帧下仍有明显 OOM 风险。FrameAveragingOperator.cs (line 82) FrameAveragingOperator.cs (line 243)
TimerStatistics、Statistics、ForEach 这些流程骨架类还存在状态串线、TTL 互删、参数名漂移、成功语义硬编码等问题，影响的是整系统稳定性。TimerStatisticsOperator.cs (line 57) StatisticsOperator.cs (line 83) ForEachOperator.cs (line 100)
JsonExtractor 受当前区域文化影响，FFT/IFFT 对 16-bit 图像不守恒，StatisticalOutlierRemoval 仍是 O(n²) brute-force KNN，离生产级大点云处理还有距离。JsonExtractorOperator.cs (line 205) FFT1DOperator.cs (line 145) StatisticalOutlierRemoval.cs (line 61)

收起
12:04



我会先把这些算子和流程骨架的实现逐个核对一遍，确认哪些问题是明确 bug、哪些还牵涉到契约调整，然后分批修复并补上针对性的测试，尽量把“会静默给出看似正常结果”的路径先收紧。


Ran 3 commands
我已经定位到所有相关实现和现有测试，接下来会先读出问题点附近的代码与测试覆盖面，优先锁定那些会“静默产出错误结果”的分支。


Ran 2 commands
第一批代码里已经能看到几个问题是实打实的：PixelToWorldTransform 目前确实把输出点包装成了 2D Position，而射线与平面求交也没有拒绝反向交点。现在继续把其余分组读完，确认哪些地方需要收紧契约、哪些地方要补算法或结果结构。


Ran 7 commands
我在继续收口问题边界。现在能确认有一类属于“实现明显偏离契约”的问题，比如 TranslationRotationCalibration 目前只读 angle 不参与求解；另一类更像“能力被写得过头”，这部分我会优先让代码和输出语义一致，避免再默默给出貌似正常的结果。


Ran 6 commands
我已经把大多数实现读通了。接下来开始动手修的顺序会是：先修那些会产出错误结果却不报错的契约问题，再补高位深与姿态/坐标输出这些会影响系统组合性的基础问题，最后再处理内存和复杂度这类稳定性问题。

我已经把问题分成四块，准备按这个顺序落地：

坐标与姿态契约
PixelToWorldTransform：拒绝反向射线交点，PixelToWorld 输出保留 Z，避免把 3D 世界点压成 2D。
ShapeMatching / RoiTransform：统一参考点与中心点的姿态语义，修掉旋转缩放后的参考点漂移。
TranslationRotationCalibration：让 angle 真正参与约束，或者在缺少一致角度信息时直接拒绝不一致输入。
静默错误结果防护
AnomalyDetection：推理时锁定 feature bank / extractor 契约，禁止被参数偷偷覆盖。
ColorDetection：LabDeltaE 没参考色时改成明确失败，不再用自参考把 DeltaE 做成 0。
SurfaceDefectDetection：对齐失败时不再继续拿未对齐参考图做差分。
PPFMatch：保证 IsMatch=true 时 FailureReason 为空，反之亦然。
高位深与数值保真
AdaptiveThreshold、Canny、DistanceTransform、HistogramEqualization：补 12/16-bit 路径，统一做动态范围感知，不再硬编码 8-bit。
NPointCalibration：仿射路径改成双精度输入，避免先降成 float。
JsonExtractor / FFT/IFFT：修区域文化解析与 16-bit 振幅守恒。
稳定性与复杂度
FrameAveraging：中值模式改掉整栈克隆 + 拼接 + 全排序。
Statistics / TimerStatistics / ForEach：清理状态隔离、TTL 清理和参数名漂移。
StatisticalOutlierRemoval：把 O(n²) 暴力 KNN 换成空间索引近邻搜索。
SubpixelEdgeDetection / EdgePairDefect / ImageStitching：先把当前能力和输出语义收紧到真实能力，避免再误导流程编排；能顺手补局部行为的会一并补。

Ran 32 commands
接下来开始改文件了，先把高位深/坐标这类基础契约打牢，再补测试；这些改动会涉及多处共享数值处理，所以我会先加一个统一的位深转换辅助，避免每个算子各自偷偷做一套。


已创建 1 个文件
我先在坐标链路上收紧了两个最危险的点：PixelToWorldTransform 的反向交点和 3D 输出丢 Z。接下来会把标定和高位深路径一并补上。


已编辑 2 个文件
标定这块我发现了一个额外的数值问题：TranslationRotationCalibration 的相似变换缩放求解分母用了均值、分子用了总和，量纲其实不一致。我会一起修掉，这样角度约束接进去后不会把 scale 再放大。


已编辑 12 个文件，ran 1 command
我正在把异常检测的推理路径改成“以训练出来的 feature bank 为准”，也就是即便流程图里还留着旧的 extractor / patch 参数，也不会再偷偷覆盖训练契约。这样至少不会再产出“像是真的”的错结果。


已编辑 5 个文件，ran 3 commands
我在处理中值内存风险和点云 O(n²) 这两块时，会优先换掉“整图大拼接”和“全量暴力邻居”这种会直接拖垮生产负载的实现；这部分改动内部会稍大一些，但对外契约尽量不动。


已编辑 2 个文件，ran 2 commands
我在 EdgePairDefect 这块不想只改文案，所以会把采样从“点到整条直线的全局距离”改成“沿配对边法向找本地边缘再量局部间距”，这样至少能对局部凹凸有感知，而不是只做一条全局宽度线。


已编辑 3 个文件
我先跑一轮聚焦测试，把现在这批修改的编译和行为问题尽早暴露出来。会用单次串行 dotnet test 把相关类合并到一个 invocation 里，避免项目锁冲突。


Ran 1 command
编译已经帮我们把几处 API 细节挑出来了：主要是 OpenCvSharp 里 MatExpr/Mat 的差异，以及我新加的位深辅助里有几个 MatType 写法需要收敛。我先把这些编译错误清掉，再继续补行为测试。


已编辑 3 个文件，ran 3 commands
已处理 40m 48s
这一轮测试已经有结果了。好消息是编译链通了，坏消息主要集中在三类：旧测试还在验证“静默容错”的旧语义、PixelToWorld 测试数据本身用了反向坐标系、以及 ForEach 的超时兼容逻辑把校验放宽过头了。我现在按这三类修正测试和个别实现。

