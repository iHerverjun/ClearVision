---
title: "Week3-修复优先级清单"
doc_type: "priority-list"
status: "active"
topic: "算子审计"
created: "2026-04-13"
updated: "2026-04-13"
---

# Week3 修复优先级清单

> 本文是 Week2 工作坊“风险收敛与 Week3 入池”的正式输出文档，只记录已确认可入池的问题、阻塞和不入池结论，不维护候选池。Week3 执行入口统一为 `docs/算子审计/启动计划/Week3-长时启动计划.md`。

## 1. 入池规则

- 只有完成证据核验的问题，才允许写入本清单。
- 每条入池问题必须同时具备：`确认结论 + 证据路径 + 复现方式 + 风险等级 + 负责人 + 截止日期`。
- 仍处于“待确认 / 待补证 / 待实验”的事项，继续留在 `Week2-审计台账.md` 或 `Week2-已升级证据链.md`，不得提前入池。
- 本清单不维护候选池，不新增 A/B 项。

## 2. P0/P1 缺陷池

| 状态 | 风险 | 来源工作坊 | 类别 | 项目 | 确认结论 | 证据路径 | 复现方式 | 阻塞项 | 负责人 | 截止日期 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| [x] | - | D | 全局 | Week2 本轮无新增 Week3 修复项 | Week3 不新增修复项，仅保留 1 条验收阻塞做闭环。 | `docs/算子审计/Week2-审计台账.md`、`docs/算子审计/Week2-审计周报.md` | `scripts/run-dotnet-test-serial.ps1` + Week2 批次复现入口 | 见阻塞清单 | D 线负责人 | 2026-04-13 |

## 3. 阻塞清单

| 状态 | 阻塞类型 | 描述 | 解除动作 | 升级路径 | 负责人 | 截止日期 | 下次检查点时间（UTC+8） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| [-] | 验收阻塞 | AI 检测已冻结 `detection-all-20260413-115808.trx` 作为初始批次，但仍缺工业签收记录，以及与批次结果一致的模型版本、阈值和环境材料。Week3 仅处理该单条阻塞，不扩展范围。 | 由 AI 检测 Owner 补齐工业签收、模型版本、阈值说明、环境说明，并与 `detection-all` 批次结果绑定；若批次一致性不成立，触发一次 `scripts/run-tests-detection-regression.ps1 -Gate all` 补跑并重绑证据。 | C 线负责人 -> AI 检测 Owner -> Week3 审核人 | C 线负责人（证据与复核） | 2026-04-24 | 2026-04-14 12:00 |

## 4. 阻塞拆解执行表（含 C3 预备执行口径）

| 状态 | 子动作 | Owner | 截止时间（UTC+8） | 输出件 |
| --- | --- | --- | --- | --- |
| [-] | C2 材料收集 | AI 检测 Owner | 2026-04-21 12:00 | 可得材料已归集并映射；仍缺工业签收实录与部分模型唯一标识/环境实值 |
| [x] | C3 执行前置条件确认 | C 线负责人（证据与复核） | 2026-04-13 18:57 | 前置条件检查单：批次可访问、来源位可定位、缺口可追责 |
| [x] | C3 证据绑定 | C 线负责人（证据与复核） | 2026-04-13 18:57 | `detection-all-20260413-115808.trx` 与模型/阈值/环境/签收映射表（含缺口标注） |
| [x] | C3 一致性核验（提前执行） | Week3 审核人 | 2026-04-13 18:57 | 核验结论：批次结果与已获证据无冲突，但签收闭环缺失，状态继续 `[-]` |
| [x] | C3 必要回归准备（仅条件触发） | C 线负责人 + AI 检测 Owner | 2026-04-13 18:57 | 判定不触发补跑：缺口为外部签收与材料闭环，非批次冲突 |
| [-] | 签收确认 | 工业签收人（业务/现场） | 2026-04-23 18:00 | 待补工业签收记录（签收结论 + 签收人 + 时间戳 + 附件索引） |
| [x] | 关单决策（C4） | C 线负责人 + Week3 审核人 | 2026-04-13 18:57 | Week3 收口完成并保留单条阻塞：进入 Week4 只追该缺口 |

## 5. C3 核验口径与触发规则

### 5.1 一致性核验口径（不等于已核验）

- 核验对象：`detection-all-20260413-115808.trx` 与签收材料中声明的模型版本、阈值、环境信息。
- 判定口径：三类信息均能一一映射且无冲突，记为“一致性成立”；任一缺失或冲突，记为“一致性不成立”。
- 当前状态：`[x]` 已于 2026-04-13 18:57（UTC+8）执行核验并形成单一审核结论。

### 5.2 触发补跑条件（仅一次）

- 仅当 C3 一致性核验结果为“不成立”时，触发一次：
- `scripts/run-tests-detection-regression.ps1 -Gate all`
- 除上述条件外，不执行补跑，不新增并行测试分支。
- 本轮核验结论：缺口来源于外部签收与材料闭环，不属于批次结果冲突，因此不触发补跑。

### 5.3 串行测试约束

- 同一 `.csproj` 禁止并发执行 `dotnet test`。
- 如需定向回归，优先通过 `./scripts/run-dotnet-test-serial.ps1` 合并过滤条件后单次执行。
- 在同一项目已完成构建的前提下，后续补跑优先加 `-NoBuild -NoRestore`，避免重复构建与锁冲突。

## 6. 执行记录

| 时间（UTC+8） | 动作 | 状态 | 说明 |
| --- | --- | --- | --- |
| 2026-04-13 17:14 | Week3 提前启动 C1 | [x] | 已完成阻塞定义与责任链字段冻结。 |
| 2026-04-13 17:14 | Week3 启动 C2 材料收集 | [-] | 已进入资料归集阶段，下一检查点 2026-04-14 12:00。 |
| 2026-04-13 17:14 | C3 执行准备建档（前置条件/口径/触发规则） | [-] | 已补齐“可直接执行”所需口径；未进行一致性核验，未触发补跑。 |
| 2026-04-13 18:57 | C2 证据映射补齐（可得项） | [-] | 已回填批次/模型/阈值/环境来源位；工业签收仍缺，维持单条阻塞。 |
| 2026-04-13 18:57 | C3 一致性核验完成 | [x] | 批次结果与现有证据一致；签收闭环缺失导致状态继续 `[-]`；不触发补跑。 |
| 2026-04-13 18:57 | C4 收口发布完成 | [x] | 已给出 Week3 结论：不关单，转入 Week4 仅追单条缺口。 |
