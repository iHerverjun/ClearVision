# ClearVision P0 可执行周计划（2026-03-06）

## 结项状态

- 状态：已完成并归档
- 结项日期：2026-03-06
- 归档位置：`docs/curation/已完成/P0可执行周计划_2026-03-06.md`
- 后续补全计划：`docs/curation/收口后续补全计划_2026-03-06.md`
- 说明：本周 P0 周计划的拆解、状态回填、关键修复与验证动作已完成；未纳入本周闭环的长期尾项已迁入后续补全计划，不再作为本周 P0 挂项。

## 说明
本文件把当前 `Active` 列表中的 5 份 `P0` 文档拆成“本周可直接执行”的任务包。

执行规则：

- 每份文档只保留 3-8 条本周动作。
- 每条动作都尽量对应到具体文件、接口或测试。
- 本周目标是“形成可验证闭环”，不是继续堆叠长清单。

---
## 执行结果（2026-03-06）

| 文档 | 结果 | 说明 |
|---|---|---|
| `docs/completed_phases/TODO_Old.md` | ✅ 已完成 | 历史修复勾选已回填，backlog 已迁出，最小发布清单已验证 |
| `docs/roadmaps/roadmap-sprint6.md` | ✅ 已完成 | Sprint 6 已改写为“实现矩阵 + 验收动作”的收口文档 |
| `docs/BUG_AUDIT_2026-03-04.md` | ✅ 已完成 | `CV-BUG-001 ~ 005` 闭环，`CV-BUG-007` 已验证通过，低优先项已降级 |
| `docs/performance_audit_2026-02-28.md` | ✅ 已结项 | 本周收口动作已完成；`#10` 与量化观测已迁入后续补全计划 |
| `docs/audits/SYSTEM_CONFIG_AUDIT_2026-02-27.md` | ✅ 已完成 | 存储策略、运行策略、PLC 占位隔离、磁盘容量与 AI 保存隔离均已回填 |

---

## 1. `docs/completed_phases/TODO_Old.md` 周计划

### 本周目标

把历史总 TODO 中仍未收口的旧尾项拆干净，避免继续混杂“已做完”和“待处理”。

### 可直接执行项

1. 逐条复核 `S6-P0-001`、`S6-P1-001~003` 的未勾选子项，把已完成内容回填到原文，消除“父项完成、子项未勾选”的状态冲突。
2. 将 `S6-P2-001` 和 `S6-P2-002` 从历史总表中移出，单独落到新 backlog，避免旧总表继续承担优化型任务。
3. 把 `S7-005 ~ S7-009` 五项顺延功能整理成单独的“流程编辑器增强待办”，不要继续挂在旧 Sprint 结项文档里。
4. 补一段“阶段 7 发布与部署”最小闭环清单，只保留本周真实要做的发布项，不再沿用 1 月份总清单口径。
5. 更新文末“问题统计”和“功能覆盖”区块，使其与当前代码基线一致。

### 完成情况（2026-03-06）

- [x] `S6-P0-001`、`S6-P1-001 ~ 003` 的子项已全部回填。
- [x] `S6-P2-001`、`S6-P2-002` 已迁入 `docs/TODO_Open_Categorized_2026-03-06.md` backlog。
- [x] `S7-005 ~ S7-009` 已拆出为独立 backlog。
- [x] 阶段 7 最小发布闭环已补齐，并通过 Release 构建 / 回归 / UI E2E 验证。
- [x] `TODO_Old.md` 已消除“已完成正文 + 未勾选子项 + 过时统计”的冲突。

---

## 2. `docs/roadmaps/roadmap-sprint6.md` 周计划

### 本周目标

把 Sprint 6 从“长规格书”收敛成“已完成项 + 真正剩余项”的可验收路线图。

### 可直接执行项

1. 按当前代码基线逐项核对 `Task 6.1 ~ 6.6`，先标出“已存在实现文件”的项，再标出“缺测试/缺收口”的项。
2. 为 `OpenAI / Azure OpenAI / Ollama` 三类连接器补一个实际完成矩阵，写清“实现已在、测试不足”还是“仍待接线”。
3. 将 `LLMConnectorFactory`、`Runtime/*`、`PromptVersionManager`、流程版本控制相关内容拆成“已完成”和“本周补验收”两列。
4. 把文档中的 `0/54` 式总量统计改造成分阶段周任务，不再把所有规格句子当成未完成任务。
5. 为 Sprint 6 增加 3 条明确验收动作：连接器 smoke test、设置页配置流测试、AI 生成链路回归测试。
6. 完成本周后，将 `roadmap-sprint6.md` 的状态改为“部分完成，待验收闭环”，避免继续显示“未完成 54 项”。

### 完成情况（2026-03-06）

- [x] `Task 6.1 ~ 6.6` 已按当前代码基线补成实现矩阵。
- [x] `OpenAI / Azure OpenAI / Ollama` 连接器矩阵已回填。
- [x] `LLMConnectorFactory`、`Runtime/*`、`PromptVersionManager`、流程版本控制已拆成“已完成 / 待验收”。
- [x] 文档状态已改为“部分完成，待验收闭环”。

---

## 3. `docs/BUG_AUDIT_2026-03-04.md` 周计划

### 本周目标

优先清掉会影响构建、执行链路和回归稳定性的缺陷。

### 可直接执行项

1. 先处理 `CV-BUG-001`：复现流程输入映射回归，补针对性测试，再修正 `Image` 端口名不一致时的数据映射逻辑。
2. 处理 `CV-BUG-002` 和 `CV-BUG-003`：修正 `Acme.OperatorLibrary` 构建配置与桌面测试工程构造函数漂移，确保构建链和测试链稳定。
3. 处理 `CV-BUG-004`：调整执行状态生命周期，保证流程结束后 `GetExecutionStatus` 还能读取最终状态。
4. 处理 `CV-BUG-005`：为 AI 测试配置文件路径加隔离，消除并发污染和文件占用问题。
5. 对 `CV-BUG-007` 进行取舍：能修就补通 E2E 前置链路，不能在本周修通就先隔离或降级，不允许继续裸挂为不稳定项。
6. 将 `CV-BUG-008`、`CV-BUG-009` 归到次周或低优先级队列，不和本周核心稳定性缺陷混做。

### 完成情况（2026-03-06）

- [x] `CV-BUG-001 ~ 005` 已形成“复现 -> 修复 -> 验证”闭环记录。
- [x] `CV-BUG-002` 已通过 Release 构建验证。
- [x] `CV-BUG-003` 已通过桌面测试验证。
- [x] `CV-BUG-007` 已通过 `npm test` 验证（4/4）。
- [x] `CV-BUG-008`、`CV-BUG-009` 已降级为后续队列。

---

## 4. `docs/performance_audit_2026-02-28.md` 周计划

### 本周目标

先止住“长期运行会炸”和“连续采集明显掉帧”两类问题。

### 可直接执行项

1. 处理 `问题 #1`：为 `ImageAcquisitionService._imageCache` 加容量上限、过期策略或显式回收，并补长时运行回归点。
2. 处理 `问题 #2`：为 `FlowExecutionService` 的调试缓存、执行状态增加清理时机和 TTL，避免内存只增不减。
3. 处理 `问题 #3`：为 ONNX `InferenceSession` 模型缓存增加释放策略，至少要有进程生命周期外的清理路径。
4. 处理 `问题 #5` 和 `问题 #10`：把连续采集每帧 PNG/Base64 这条重链路拆开，优先减少不必要的编码与复制。
5. 处理 `问题 #4` 和 `问题 #6`：核查相机连续采集的资源释放与固定大缓冲区策略，避免额外 GC/Pin 压力。
6. 补一份“修复后验证清单”，至少包含内存曲线、连续采集帧率、模型切换后的句柄释放三个观测项。

### 完成情况（2026-03-06）

- [x] `问题 #1 ~ #6` 已在原审计文档中回填为“已修复 / 已验证”。
- [x] 已明确 `#10` 仍为主要剩余项，未误标完成。
- [x] 量化观测项已迁入 `docs/curation/收口后续补全计划_2026-03-06.md`，不再作为本周 P0 挂项。

---

## 5. `docs/audits/SYSTEM_CONFIG_AUDIT_2026-02-27.md` 周计划

### 本周目标

把“可保存但不生效”的配置项，至少打通到最关键的运行时消费链路。

### 可直接执行项

1. 优先接通 `SavePolicy` / `ImageSavePath` 到结果图保存链路，确保存储策略真正影响写盘行为。
2. 接通 `AutoRun` / `StopOnConsecutiveNg` 到运行控制逻辑，形成“配置驱动运行”的第一条闭环。
3. 给 PLC 通讯页的未实现功能补 `disabled + tooltip`，明确标出“开发中”，避免误导现场用户。
4. 处理“保存常规设置”按钮：删除重复入口，或单独绑定仅保存 General 的逻辑，二选一，不再维持空按钮。
5. 把磁盘容量卡改成真实接口数据，替换硬编码占位值。
6. 调整设置页保存流程：至少先把 AI 保存与全局设置保存隔离，避免一次保存触发隐式副作用。

### 完成情况（2026-03-06）

- [x] `SavePolicy / ImageSavePath` 已确认接入运行时。
- [x] `AutoRun / StopOnConsecutiveNg` 已确认接入运行控制。
- [x] PLC 未实现动作已显式 `disabled + tooltip`。
- [x] 磁盘容量卡已接入真实接口数据。
- [x] 全局保存已完成 AI 隐式保存隔离。

---

## 建议执行顺序

1. `BUG_AUDIT`：先稳构建和执行链路。
2. `performance_audit`：再止住高频内存与采集性能问题。
3. `SYSTEM_CONFIG_AUDIT`：把关键配置接入运行时。
4. `roadmap-sprint6`：回填已做项并压实验收动作。
5. `TODO_Old`：最后做历史文档清账与拆分。





