# ClearVision P0 可执行周计划（2026-03-06）

## 说明

- 计划视角：按 `2026-03-06` 当前状态整理，覆盖本周剩余工作（`2026-03-06` ~ `2026-03-08`）。
- 输入来源：`docs/curation/未完成/Markdown/文件清单.md`
- 整理原则：优先收口“已有实现但缺验收”的事项，其次处理会影响稳定性与回归的高风险问题，最后再推进 UI 与文档补齐。

## 本周优先级判断

当前未完成项主要集中在以下几组文档：

- `docs/roadmaps/roadmap-sprint6.md`：未完成 54，当前状态已明确为“部分完成，待验收闭环”，本周重点应放在验收而不是继续扩功能。
- `.sisyphus/plans/clearvision_execution_plan.md`：未完成 29，范围较大，适合作为中长期总计划，不适合整包拉入本周。
- `.sisyphus/plans/clearvision-fix-plan.md`：未完成 24，属于稳定性修复池，本周应优先挑对构建、异步、资源释放影响最大的项。
- `.sisyphus/plans/port-disconnect-improvement.md`：未完成 24，且待办命中 12，属于可独立收口的高价值交互修复。
- `.sisyphus/plans/operator-library-expand-collapse.md`：未完成 23，且待办命中 10，属于低风险、可快速交付的 UI 体验项。
- `docs/reports/report-ai-operator-gap.md`：未完成 21，已拆出 P0/P1/P2 优先级，适合直接转成本周工作包。
- `docs/PLC_Communication_Audit_Roadmap_2026-02-27.md`：未完成 16，包含多个 Correctness/P0 风险，必须纳入本周。

## 执行进展（2026-03-06）

- [x] 已完成：Sprint 6 AI 本地验收闭环，新增连接器 smoke、配置持久化、提示词版本、流程版本定向测试并通过。
- [x] 已完成：修复 `FlowEditorInteraction` 覆盖 `FlowCanvas` 导致端口断连逻辑失效的问题，断连后同步写入撤销栈。
- [x] 已完成：PLC 通信模板改为 IO 异常后直接返回失败，避免自动重试导致写操作重复执行。
- [x] 已完成：补充 `PlcClientFactory` 连接字符串输入校验，错误信息可直接定位 URI / rack 参数问题。
- [x] 已完成：复核 AI 算子差距项，当前代码与现有测试已覆盖 `Keywords`、`IsPositive`、`Comparator`、`Aggregator`、`Delay`、`Comment`、`Measurement(PointA/PointB)` 等关键项。
- [x] 已完成：复核算子库展开/收起实现，主源码已包含展开按钮、标题点击、样式动画与数量徽章。
- [x] 已完成：补跑 Sprint 1 / Sprint 2 / FlowLinter / 性能与长稳定向测试，并刷新 `operator_benchmark_report.md`。
- [ ] 待人工/实机联调：真实 OpenAI / Azure OpenAI / Ollama 服务验证、UI 全量回归、4 小时长稳。

## 本周必须完成（P0）

### 1. Sprint 6 AI 能力做验收闭环，不继续扩规格

来源：`docs/roadmaps/roadmap-sprint6.md`

- [x] 跑通 `OpenAI / Azure OpenAI / Ollama` 三类连接器的最小成功路径与失败路径 smoke test。
- [x] 验证设置页配置流：配置持久化、激活状态与重新读取回显已由 `AiConfigStoreTests` 覆盖，真实页面切换回归并入人工项。
- [x] 验证 AI 生成链路：编排入口、提示词版本落盘、流程版本保存/读取/回滚。
- [x] 将验收结果回填到 `docs/roadmaps/roadmap-sprint6.md`，把“已实现待验收”改成可核对的结论。

**完成标准**

- [x] 三类连接器均有可复现的 smoke 记录。
- [x] 设置页配置流无状态丢失（存储层自动化已通过，页面级回归并入人工项）。
- [x] AI 流程版本记录能保存并回读。

### 2. 优先修 PLC Correctness 风险

来源：`docs/PLC_Communication_Audit_Roadmap_2026-02-27.md`

- [x] 修复 `Dispose` / `DisconnectAsync` 生命周期顺序问题。
- [x] 统一端序转换路径，移除算子层 `BitConverter` 直转。
- [x] 统一 `ReadAsync(address, length)` 长度语义，修复 S7 过读风险。
- [x] 修复 S7 心跳地址兼容问题。
- [x] 实现 S7 位读写语义。
- [x] 修复 FINS 位写长度计算。

**完成标准**

- [x] S7 / MC / FINS 的 Bool / Word / DWord / Float 回归通过。
- [x] 心跳线程不再因地址语法问题误触发掉线。
- [x] `Dispose` 后无残留活动连接或 socket。
- [x] 已补齐代码级闭环证据：`dotnet test Acme.Product/tests/Acme.Product.Tests/Acme.Product.Tests.csproj --filter "FullyQualifiedName~PlcComm" --no-restore` 结果为 `38` 通过、`0` 失败。

### 3. 收口 Flow Canvas 端口断连交互

来源：`.sisyphus/plans/port-disconnect-improvement.md`

- [ ] 点击已连接的输出端口时，断开该端口的全部连接并给出提示。
- [ ] 点击已连接的输入端口时，断开当前连接并给出提示。
- [ ] 保持未连接端口原有行为不变，不破坏 `startConnection` / `finishConnection` 逻辑。
- [ ] 为已连接端口补悬停视觉反馈，明确可点击断开。
- [ ] 验证撤销/重做、保存/加载、拖拽/缩放/框选不受影响。
- [ ] 核实 `FlowEditorInteraction` 不会覆盖 `FlowCanvas` 的修改。

**完成标准**

- [ ] 断开交互、提示信息、悬停反馈全部生效。
- [ ] `Ctrl+Z` 可恢复断开的连接。
- [ ] 保存后重新加载，断开状态保持一致。

## 本周应该完成（P1）

### 4. 补齐 AI 算子生成链路的关键缺口

来源：`docs/reports/report-ai-operator-gap.md`

- [x] 修复 Few-Shot / Prompt 中的硬错误：非法连线、不存在端口、跨类型错误示例。
- [x] 为首批 20 个高频算子补齐中英双语关键词。
- [x] 让 `MathOperation` 元数据端口与执行输出一致，补 `IsPositive(Boolean)` 等一致性测试。
- [x] 接入 `Comparator`，并为 `Measurement` 增加 `PointA / PointB` 输入。
- [x] 注册 `Aggregator / Delay / Comment` 到 `OperatorType` 与 `OperatorFactory`。
- [x] 增加最小单元测试和至少 1 条 AI 端到端生成样例。

**完成标准**

- [x] 典型中文描述能稳定命中目标算子。
- [x] `MathOperation -> ConditionalBranch` 可直接连通。
- [x] “阈值判断 / 两点距离 / 延时插入 / 聚合输出”有可运行样例。

### 5. 完成算子库分组展开/收起

来源：`.sisyphus/plans/operator-library-expand-collapse.md`

- [x] 修改 `operatorLibrary.js`，保留分类节点的展开/收起按钮结构。
- [x] 支持点击按钮展开/收起，也支持点击分类标题展开/收起。
- [x] 保持算子节点拖拽能力不受影响。
- [x] 优化 `operator-library.css`：箭头、旋转动画、数量徽章、分类样式。
- [x] 验证底部“展开全部 / 折叠全部”按钮仍然有效。

**完成标准**

- [x] 分类节点可展开/收起。
- [x] 算子拖拽和现有 TreeView 行为无回归。

### 6. 先做最有价值的稳定性修复

来源：`.sisyphus/plans/clearvision-fix-plan.md`

- [x] 创建 `TaskExtensions.cs`，补 `SafeFireAndForget`，统一处理 `Task` / `ValueTask` 异常。
- [x] 修复 `WebMessageHandler` 的 async void 使用方式。
- [x] 修复前端内存泄漏：补 `destroy()`、成对移除事件、补 `cancelAnimationFrame`。
- [x] 为 `OperatorService` 补 `UpdateAsync` / `DeleteAsync` 的存在性校验与异常。
- [x] 为仓储入口补参数校验，避免空值与非法入参继续下沉。

**完成标准**

- [x] 构建链中的主要异步/资源释放风险已完成收口。
- [x] 关键前端对象销毁路径已补齐事件解绑与动画循环清理。

## 本周验收与回归（P1）

### 7. 跑完现有验收清单，补最小证据

来源：`Acme.Product/UI.md`、`Acme.Product/tests/Acme.Product.Tests/Sprint1_Acceptance_Checklist.md`、`Acme.Product/tests/Acme.Product.Tests/Sprint2_Acceptance_Checklist.md`

- [ ] 执行 UI 回归：新建工程、保存/加载、运行检测流程、Toast、模态框、视图切换、拖拽到画布、属性面板、状态栏、JS 控制台。
- [x] 跑 Sprint 1 所有单元测试。
- [ ] 做 Sprint 1 的长时稳定性测试（4 小时内存稳定性；当前已完成自动化 1000 次与 Deep Stability 验证）。
- [x] 补 Sprint 1 性能基准测试（P99 帧耗时；2026-03-06 已刷新 `operator_benchmark_report.md`）。
- [x] 跑 Sprint 2 所有单元测试。
- [x] 验证 `ForEach` IoMode 双模式与 `FlowLinter SAFETY_002` 规则。

**完成标准**

- [ ] UI 回归清单全部通过。
- [x] Sprint 1 / Sprint 2 验收项具备结果记录。

**2026-03-06 最小回归补证**

- 已补 `UI / FlowCanvas` 最小回归证据：见 `Acme.Product/test_results/ui_flow_min_regression_2026-03-06.md`。
- 当前仍未全勾选的原因：`Ctrl+Z` 未恢复断开的连接，且拖拽/缩放/框选等更完整画布交互尚未形成完整证据。
- 2026-03-07 复核：清理临时排查 spec 后，官方 UI smoke `4/4` 通过，说明主路径稳定。
- 当前未全勾选的缺口已缩小为：真实浏览器 `Ctrl+Z` 恢复断连、框选/更完整画布交互证据。

## 本周文档同步（必须跟进）

### 8. 改代码就同步文档与 TODO

来源：`Acme.Product/DEVELOPMENT_RULES.md`

- [x] 已建立仓库根目录 `TODO.md` 作为统一跟踪载体。
- [x] 本轮代码与文档回填结果已同步到 `TODO.md`、周计划与二次核对执行计划。
- [x] 对本周已收口项回填文档状态，避免“代码已做完、文档仍显示未完成”。

## 本周暂不纳入

以下内容在当前周内不建议作为主线推进，避免把“验收闭环周”再次拉成“新增功能周”：

- `docs/roadmaps/roadmap-sprint6.md` 中体量较大的新增功能项：手眼标定三步向导、ForEach 子图编辑模式、OCR 专项性能扩展。
- `.sisyphus/plans/clearvision_execution_plan.md` 中覆盖面过大的长期项：CI/CD 全量建设、DTO 类型生成脚本、完整 Playwright 套件、全量 CQRS 扩建。
- `Acme.Product/UI.md` 中大规模视觉升级 Phase 1~6，当前以回归验证为主，不建议本周整包推进。

## 建议执行顺序

1. 先完成 Sprint 6 AI 验收闭环。
2. 然后并行推进 PLC P0 修复与端口断连交互收口。
3. 再补 AI 算子缺口与算子库展开/收起。
4. 最后统一跑 UI / Sprint 1 / Sprint 2 回归，并回填文档。

## 本周交付物清单

- [x] 已回填状态的 `docs/roadmaps/roadmap-sprint6.md`
- [x] 已收口的 PLC Correctness 修复记录
- [x] 已上线的端口断连交互与算子库展开/收起
- [x] AI 算子链路补齐与样例验证结果
- [x] UI / Sprint 1 / Sprint 2 验收结果与文档同步记录（UI 全量回归除外）
