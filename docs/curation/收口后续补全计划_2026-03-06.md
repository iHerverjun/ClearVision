# ClearVision 收口后续补全计划（2026-03-06）

## 说明

本计划承接 `docs/curation/已完成/P0可执行周计划_2026-03-06.md` 结项后仍需持续推进的长期尾项。

原则：

- 不回退已完成的 P0 周计划结论。
- 只承接“本周未纳入闭环”的技术尾项与验收尾项。
- 每项都要求有明确文件落点、验证动作与收口标准。

## 1. 发布与历史尾项收口

### 目标

把 `docs/completed_phases/TODO_Old.md` 从“部分完成”推进到真正可归档状态。

### 可直接执行项

1. 把阶段 7 最小发布闭环拆成独立验收表，只保留真实发布前置项。
2. 复核 Release 构建、桌面测试、关键 UI 回归是否已有证据，缺的只补证据，不再扩范围。
3. 将仍属长期演进的编辑器增强项继续留在 backlog，不再挂回历史总表。
4. 在 `TODO_Old.md` 中补一段“阶段 7 结项结论”，明确哪些是发布闭环、哪些已迁出。

## 2. Sprint 6 AI 验收收口

### 目标

把 `docs/roadmaps/roadmap-sprint6.md` 从“部分完成，待验收闭环”推进到可关闭状态。

### 可直接执行项

1. 跑一轮 `OpenAI / Azure OpenAI / Ollama` 连接器 smoke，分别记录配置可达性、错误提示和超时表现。
2. 复核设置页 AI 配置流，确认 `AiConfigStore` 持久化、切换模型、保存草稿隔离链路闭环。
3. 复核 AI 生成链路回归，至少覆盖提示词版本、运行时工厂选型和历史版本管理。
4. 将三类验收结果写回 `roadmap-sprint6.md` 的“本周收口动作”列，避免长期停留在“待验收”。

## 3. 性能尾项与量化观测

### 目标

处理 `docs/performance_audit_2026-02-28.md` 中当前唯一明确保留的主尾项：连续采集路径的复制与 Base64 开销。

### 可直接执行项

1. 审查 `Acme.Product/src/Acme.Product.Infrastructure/Services/ImageAcquisitionService.cs` 中单帧与连续采集的 `Convert.ToBase64String(...)` 路径，区分“必须返回 UI 的链路”和“仅内部使用的链路”。
2. 优先消除连续采集中的无条件 Base64 生成，必要时改成按订阅方/调用方选择性编码。
3. 增补一轮量化观测：内存曲线、连续采集帧率、关键句柄数量，形成新的验证记录。
4. 收口后把 `performance_audit_2026-02-28.md` 中 `#10` 从“部分完成”改为闭环结论。

## 4. 系统配置保存流程收口

### 目标

把 `docs/audits/SYSTEM_CONFIG_AUDIT_2026-02-27.md` 中剩余的配置收口项压到最小。

### 可直接执行项

1. 处理设置页保存流程事务化，避免多步骤保存出现部分成功窗口。
2. 确认 `softwareTitle` / `autoStart` 是否需要真实运行时消费者；若本轮不接线，则降级为显式占位并在文档中说明。
3. 复核常规设置、PLC、文件存储、运行控制四类配置在“保存后立刻生效”语义上的一致性。
4. 在审计报告中更新“最高优先级剩余项”，只保留真实还未完成的条目。

## 5. 文档与测试基线治理

### 目标

让源文档状态、验收清单和基线产物策略保持一致，不再出现“索引已完成，原文未同步”的冲突。

### 可直接执行项

1. 提交 `Acme.Product/tests/Acme.Product.Tests/Sprint1_Acceptance_Checklist.md` ~ `Sprint4_Acceptance_Checklist.md` 的状态回填，消除与单页总索引的冲突。
2. 单独提交 `Acme.OperatorLibrary/Acme.OperatorLibrary.csproj` 与 `CameraProviderAdapterTests.cs`，让 `BUG_AUDIT` 中已关闭项真正落仓。
3. 决定 `Acme.Product/test_results/operator_benchmark_report.md` 的策略：若保留为基线文件，则补运行环境说明；若仅作本地产物，则改出仓库跟踪策略。
4. 清理仍留在仓库中的历史错误转储与一次性排障文件，避免下一轮审计继续误判。
