# BlobDetection 技术笔记

> **对应算子**: `BlobDetectionOperator` / `OperatorType.BlobAnalysis`  
> **OperatorType**: `OperatorType.BlobAnalysis`  
> **代码依据**: `Acme.Product/src/Acme.Product.Infrastructure/Operators/BlobDetectionOperator.cs`  
> **相关算子**: [Threshold](./04-Threshold-技术笔记.md)、[Morphology](./10-Morphology-技术笔记.md)、[FindContours](./12-FindContours-技术笔记.md)  
> **阅读前置**: 本文档从零基础讲起，无需前置知识  
> **核心来源**: ClearVision 当前实现、OpenCV connected components / contours、经典形状特征教材

---

## 1. 一句话先理解这个算子

`BlobDetection` 的本质不是"识别某类物体"，而是把前景区域一个个分出来，再给每个区域算面积、圆度、凸度、孔洞数等特征。

---

## 2. 基础概念：什么是连通域？

### 2.1 用"洪水填充"理解连通域

想象你有一张二值图像（只有黑白两色）：

```
白 白 黑 黑 白 白
白 黑 黑 黑 白 白
白 白 黑 白 白 白
白 白 白 白 黑 黑
白 白 白 白 黑 黑
```

现在想象你有一桶红色的水，往某个黑色像素上倒。水会**沿着相邻的黑色像素蔓延**开来，所有能被水淹到的像素就构成一个**连通域**。

倒一次水 = 找到一个连通域。不同的连通域之间被白色像素隔开。

### 2.2 4连通 vs 8连通

水的"流动规则"决定了连通域的划分方式：

**4连通（四连通）**：水只能向上下左右4个方向流动

```
  ↑
← ● →
  ↓
```

**8连通（八连通）**：水可以向上、下、左、右 + 4个对角线方向流动

```
↖ ↑ ↗
← ● →
↙ ↓ ↘
```

**示例对比**：

```
场景：两条线对角相交
黑 白 白 黑
白 黑 黑 白
白 黑 黑 白
黑 白 白 黑

4连通判断：两条独立的线（对角不相连）
8连通判断：连在一起形成一个X形
```

> 💡 **实际应用提示**：BlobDetection 算子默认使用 8 连通，这在大多数工业检测场景中能更好地保持目标完整性。

---

## 3. 算法原理

### 3.1 什么是 blob

在这里，blob 更接近"一个连通的前景区域"。  
只要前景像素通过连通关系连成一片，就会被视为一个候选区域。

### 3.2 当前实现做了两层工作

1. **连通域提取**  
   先把前景区域切成一个个独立 component
2. **区域特征分析**  
   再给每个区域算面积、周长、圆度、凸度、矩形度、惯性比、离心率、孔洞数等

### 3.3 为什么它常接在阈值和形态学后面

因为它很依赖输入前景质量。  
如果前面 [Threshold](./04-Threshold-技术笔记.md) 或 [Morphology](./10-Morphology-技术笔记.md) 做得不好，blob 结果就会直接失真。

---

## 4. Blob特征详解

### 4.1 基础几何特征

#### 面积 (Area)
**含义**：blob 包含的像素总数  
**公式**：`Area = Σ(属于blob的像素)`  
**单位**：像素个数  
**用途**：最基本的筛选条件，过滤噪点和过大背景

```
示例：一个5×5的实心方块
■ ■ ■ ■ ■
■ ■ ■ ■ ■
■ ■ ■ ■ ■
■ ■ ■ ■ ■
■ ■ ■ ■ ■
Area = 25
```

#### 周长 (Perimeter)
**含义**：blob 外边界的长度  
**计算方式**：统计外轮廓边缘像素数，或使用 Freeman 链码累加  
**用途**：结合面积计算圆度等形状特征

```
示例：4×4方块
■ ■ ■ ■
■ □ □ ■  (□表示内部，不计算周长)
■ □ □ ■
■ ■ ■ ■

外边界像素数 = 12（外围一圈）
```

### 4.2 形状描述特征

#### 圆度 (Circularity)
**含义**：衡量形状有多"圆"的指标  
**公式**：
```
Circularity = 4π × Area / Perimeter²
```
**取值范围**：0 ~ 1  
- `1.0` = 完美圆形
- `0.785` ≈ 正方形
- 越小 = 形状越不规则

**直观理解**：
```
完美圆形:    Circularity = 1.0     (○)
正方形:      Circularity ≈ 0.785   (□)
长条形:      Circularity ≈ 0.2     (▭)
星形:        Circularity < 0.1     (☆)
```

#### 凸度 (Convexity)
**含义**：衡量形状"凹陷程度"的指标  
**需要先理解凸包**：把形状想象成钉子板上的橡皮筋，绷紧后包围形状的轮廓就是凸包

**公式**：
```
Convexity = Area / ConvexHullArea
```
**取值范围**：0 ~ 1  
- `1.0` = 完全凸形，没有任何凹陷
- 越小 = 凹陷越严重

**示例**：
```
原始形状        凸包           计算
  ■ ■            ■ ■ ■
■     ■   →    ■     ■    Convexity < 1
  ■ ■            ■ ■ ■
 (月牙形)       (凸包更大)

圆形/矩形:  Convexity = 1.0
C形零件:    Convexity ≈ 0.8
带缺口零件: Convexity < 0.5
```

#### 矩形度 (Rectangularity)
**含义**：衡量形状有多像矩形的指标  
**公式**：
```
Rectangularity = Area / (BoundingBoxWidth × BoundingBoxHeight)
```
**取值范围**：0 ~ 1  
- `1.0` = 完美填充其外接矩形
- 越小 = 形状与外接矩形差距越大

**注意**：基于**轴对齐**外接矩形，旋转矩形会被低估

```
轴对齐外接矩形：        旋转后的矩形：
┌─────────────┐        ┌─────┐
│   ■ ■ ■     │        │  /│
│   ■ ■ ■     │   vs   │ / │  (矩形度被低估)
│   ■ ■ ■     │        │/  │
└─────────────┘        └───┘
Rectangularity=1.0     Rectangularity≈0.5
```

### 4.3 惯性特征

#### 惯性比 (Inertia Ratio)
**含义**：描述形状"拉长程度"的指标  
**物理意义**：类似物理中的转动惯量，描述质量分布

**公式**：
```
Inertia Ratio = λ_min / λ_max
```
其中 λ_min、λ_max 是二阶矩矩阵的特征值

**取值范围**：0 ~ 1  
- `1.0` = 各向同性（圆形、正方形）
- 接近 `0` = 极度细长

```
圆形/正方形: Inertia Ratio = 1.0
椭圆:        Inertia Ratio ≈ 0.5 (短轴/长轴)
细长线:      Inertia Ratio ≈ 0.01
```

#### 离心率 (Eccentricity)
**含义**：描述形状偏离圆形的程度  
**公式**：
```
Eccentricity = √(1 - (λ_min/λ_max)²)
```
**取值范围**：0 ~ 1  
- `0` = 完美圆形
- 接近 `1` = 极度细长

### 4.4 特征对比表

| 特征 | 用途 | 完美圆形值 | 典型应用 |
|------|------|-----------|---------|
| Circularity | 找圆/球体 | 1.0 | 轴承滚珠检测 |
| Convexity | 检测凹陷缺陷 | 1.0 | 缺口检测、裂纹 |
| Rectangularity | 找矩形物体 | 1.0 | PCB元件检测 |
| Inertia Ratio | 区分长短 | 1.0 | 针脚方向判断 |
| Eccentricity | 测量椭圆程度 | 0 | 细胞形态分析 |

---

## 5. 特征筛选过程

### 5.1 筛选流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    原始图像输入                              │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 1: 二值化                                             │
│  Threshold → 前景(白) vs 背景(黑)                           │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 2: 形态学处理 (可选)                                   │
│  开运算/闭运算 → 去噪、连接断裂                              │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 3: 连通域标记                                          │
│  ConnectedComponents → 每个blob分配ID                       │
│  例: Blob#1, Blob#2, Blob#3, Blob#4...                      │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 4: 特征计算 (对所有blob)                               │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           │
│  │Blob#1   │ │Blob#2   │ │Blob#3   │ │Blob#4   │           │
│  │Area=500 │ │Area=50  │ │Area=2000│ │Area=300 │           │
│  │Circ=0.9 │ │Circ=0.3 │ │Circ=0.8 │ │Circ=0.95│           │
│  │...      │ │...      │ │...      │ │...      │           │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘           │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 5: 特征筛选                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 筛选条件:                                            │   │
│  │   MinArea = 100   → 过滤 Blob#2 (Area=50)           │   │
│  │   MaxArea = 1000  → 过滤 Blob#3 (Area=2000)         │   │
│  │   MinCirc = 0.8   → 全部通过                        │   │
│  └─────────────────────────────────────────────────────┘   │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 6: 输出结果                                            │
│  通过: Blob#1, Blob#4                                       │
│  过滤: Blob#2 (太小), Blob#3 (太大)                         │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 参数调优策略

```
调试阶段建议:

阶段1 - 面积筛选（先去掉明显错误）
  MinArea: 从0开始逐步增加，直到噪点消失
  MaxArea: 设一个明显大于目标的值

阶段2 - 形状筛选（精细化）
  如果目标形状稳定，添加圆度/凸度约束
  
阶段3 - 组合筛选（处理复杂场景）
  使用 FeatureFilter 表达式进行多条件组合
```

---

## 6. 孔洞与欧拉数

### 6.1 什么是孔洞？

**孔洞定义**：blob 内部被背景像素包围的封闭区域

```
实心圆环 vs 空心圆环:

实心:              带孔洞:
  ■ ■ ■ ■ ■          ■ ■ ■ ■ ■
■ ■ ■ ■ ■ ■ ■      ■ ■ □ □ ■ ■ ■
■ ■ ■ ■ ■ ■ ■      ■ ■ □ □ ■ ■ ■
■ ■ ■ ■ ■ ■ ■      ■ ■ □ □ ■ ■ ■
  ■ ■ ■ ■ ■          ■ ■ ■ ■ ■

HoleCount = 0      HoleCount = 1
```

### 6.2 欧拉数 (Euler Number)

**定义**：描述拓扑结构的数字  
**公式**：
```
EulerNumber = 连通域数量 - 孔洞数量
            = Components - Holes
```

**拓扑意义**：欧拉数在拉伸、压缩等连续变形下保持不变（只要不撕裂或粘合）

**示例**：

```
形状                Components    Holes    EulerNumber
─────────────────────────────────────────────────────────
实心圆              1             0        1
圆环                1             1        0
数字"8"             1             2        -1
两个分离的圆        2             0        2
两个分离的圆环      2             2        0
字母"A"             1             1        0
字母"B"             1             2        -1
```

### 6.3 实际应用

**用欧拉数分类**：
```
EulerNumber = 1  → 实心物体（螺丝、垫片）
EulerNumber = 0  → 单孔物体（垫圈、螺母）
EulerNumber < 0  → 多孔物体（特殊零件、字符"8"）
```

**ClearVision 实现**：
- 带孔目标默认仍算 **1 个 blob**
- 孔洞通过 `HoleCount` 和 `EulerNumber` 反映
- 如需分离孔洞，使用专门的孔洞分析功能

---

## 7. 与 FindContours 对比

### 7.1 核心区别

```
┌─────────────────┬────────────────────────┬────────────────────────┐
│     特性        │     BlobDetection      │      FindContours      │
├─────────────────┼────────────────────────┼────────────────────────┤
│ 核心概念        │ 连通域 (区域)          │ 轮廓 (边界线)          │
│ 输出单位        │ 完整区域 + 统计特征    │ 边界点序列             │
│ 孔洞处理        │ 保留孔洞信息           │ 可获取孔洞轮廓         │
│ 特征计算        │ 内置面积/圆度/凸度等   │ 需自行计算             │
│ 筛选能力        │ 参数化自动筛选         │ 需后处理筛选           │
│ 内存占用        │ 较大（保留mask）       │ 较小（仅轮廓点）       │
│ 适用场景        │ 计数、粗分类、筛选     │ 精确定位、形状匹配     │
└─────────────────┴────────────────────────┴────────────────────────┘
```

### 7.2 视觉对比

```
输入图像:              BlobDetection:           FindContours:
  ■ ■ ■ ■ ■            [区域1]                  外轮廓:
■ ■ □ □ ■ ■ ■            面积: 21                 ●───●
■ ■ □ □ ■ ■ ■            周长: 16                 │ □ □ │
  ■ ■ ■ ■ ■              圆度: 0.85               ●───●
                         凸度: 1.0
                         孔洞: 1
```

### 7.3 选择指南

**选择 BlobDetection 当**：
- ✅ 需要统计特征（面积、圆度等）进行筛选
- ✅ 需要计数多个目标
- ✅ 需要根据形状特征过滤
- ✅ 处理带孔物体时需要整体分析

**选择 FindContours 当**：
- ✅ 需要精确的边界点坐标
- ✅ 需要进行形状匹配/模板匹配
- ✅ 需要分析每个孔洞的独立轮廓
- ✅ 内存敏感，只需边界信息

### 7.4 组合使用

```
最佳实践 - 粗筛 + 精定位:

图像 → BlobDetection(粗筛) → 找到候选区域
                            ↓
                    对每个候选区域
                            ↓
                    FindContours(精定位)
                            ↓
                    亚像素边缘/形状匹配
```

---

## 8. 实战案例

### 8.1 案例一：零件计数

**场景**：传送带上散落的螺丝钉计数

```
完整流程:

Step 1: 图像采集
┌─────────────────┐
│ ●   ●     ●     │  螺丝钉散落在传送带
│   ●     ●   ●   │  背景较均匀
│ ●     ●     ●   │
└─────────────────┘

Step 2: 预处理 - 阈值分割
┌─────────────────┐
│ ■   ■     ■     │  二值化
│   ■     ■   ■   │  前景=螺丝钉(白)
│ ■     ■     ■   │  背景=黑色
└─────────────────┘

Step 3: 形态学处理 (开运算去噪)
┌─────────────────┐
│ ■   ■     ■     │  去除小噪点
│   ■     ■   ■   │  平滑边界
│ ■     ■     ■   │
└─────────────────┘

Step 4: BlobDetection 参数配置
┌────────────────────────────────────────┐
│ MinArea = 500      (过滤灰尘)         │
│ MaxArea = 5000     (过滤重叠)         │
│ MinCircularity = 0.7 (螺丝大致圆形)   │
│ MinConvexity = 0.9 (排除破损件)       │
└────────────────────────────────────────┘

Step 5: 结果输出
BlobCount = 7
每个blob的中心坐标用于机械手抓取
```

### 8.2 案例二：缺陷检测（凹陷检测）

**场景**：检测零件表面是否有凹陷缺陷

```
完整流程:

Step 1: 正常零件 vs 缺陷零件

正常零件:              缺陷零件(凹陷):
  ■ ■ ■ ■ ■              ■ ■   ■ ■
■ ■ ■ ■ ■ ■ ■          ■ ■ ■ ■ ■ ■ ■
■ ■ ■ ■ ■ ■ ■          ■ ■ □ □ ■ ■ ■
  ■ ■ ■ ■ ■              ■ ■ ■ ■ ■

Step 2: 关键特征选择
┌─────────────────────────────────────────────┐
│ 凹陷会导致凸度下降!                         │
│                                             │
│ 正常零件: Convexity = 1.0                   │
│ 缺陷零件: Convexity = 0.85 (凹陷区域)       │
└─────────────────────────────────────────────┘

Step 3: BlobDetection 参数配置
┌────────────────────────────────────────┐
│ MinArea = 1000                        │
│ MaxArea = 10000                       │
│ MinConvexity = 0.95  ★关键参数        │
│  (低于此值判定为缺陷)                 │
└────────────────────────────────────────┘

Step 4: 缺陷判定逻辑
如果 Blob.Convexity < 0.95:
    → 标记为 "有凹陷缺陷"
    → 输出缺陷位置坐标
```

### 8.3 案例三：细胞分析

**场景**：显微镜下细胞形态分析

```
完整流程:

Step 1: 图像特点
┌─────────────────────────────────┐
│    ○      ◐○      ○            │
│  ○   ○    ○   ◐○    ○          │  ○=正常细胞
│    ◐○      ○      ◐○           │  ◐=分裂中细胞
│  ○      ○○      ○              │  (形状更圆)
└─────────────────────────────────┘

Step 2: 预处理
- 使用自适应阈值处理光照不均
- 开运算分离粘连细胞

Step 3: 多特征联合分析
┌────────────────────────────────────────────────┐
│ 特征组合识别细胞状态:                          │
│                                                │
│ 正常细胞:                                      │
│   - Area: 500-800                              │
│   - Circularity: 0.85-0.95                     │
│   - Convexity: > 0.95                          │
│                                                │
│ 分裂中细胞:                                    │
│   - Area: 800-1200 (更大)                      │
│   - Circularity: < 0.8 (形状不规则)            │
│   - Convexity: < 0.9 (凹陷)                    │
│                                                │
│ 死细胞/碎片:                                   │
│   - Area: < 200                                │
│   - Circularity: 不定                          │
└────────────────────────────────────────────────┘

Step 4: 分类输出
使用 FeatureFilter 表达式:
"Area>500 AND Area<800 AND Circularity>0.85" → 正常细胞
"Area>800 AND Convexity<0.9"                  → 分裂细胞
```

### 8.4 案例四：PCB元件检测

**场景**：检测PCB板上元件是否正确放置

```
完整流程:

Step 1: 元件类型识别

电阻(矩形):            电容(圆形):           IC芯片(带脚):
┌─────┐                  ●●●                  ┌──────────┐
│■■■■■│                ●■■■●                 │■■■■■■■■■■│
│■■■■■│                ●■■■●                 │■■■■■■■■■■│
│■■■■■│                  ●●●                  │■■■■■■■■■■│
└─────┘                                       └──────────┘
                                             □ □ □ □ □ □ □

Step 2: 使用矩形度区分
┌────────────────────────────────────────────────┐
│ 电阻:  Rectangularity ≈ 0.95                   │
│ 电容:  Rectangularity ≈ 0.78 (圆形外接矩形)    │
│ IC:    Rectangularity ≈ 0.85 (含引脚)          │
└────────────────────────────────────────────────┘

Step 3: 参数配置策略
┌────────────────────────────────────────────────┐
│ 电阻检测:                                      │
│   MinRectangularity = 0.9                      │
│   MinInertiaRatio = 0.3 (允许细长)             │
│                                                │
│ 电容检测:                                      │
│   MinCircularity = 0.8                         │
│   MinRectangularity = 0.6 (不高)               │
│                                                │
│ IC检测:                                        │
│   MinArea = 2000                               │
│   MinRectangularity = 0.8                      │
│   EulerNumber = 0 (带孔-芯片本体)              │
└────────────────────────────────────────────────┘
```

---

## 9. 当前实现口径

在 ClearVision 当前实现里，它的主路径不是"直接全图 `findContours`"，也不是单纯的 `SimpleBlobDetector`。

当前主执行路径更接近：

```text
图像
  -> 灰度化
  -> 二值化
  -> 可选黑白反转
  -> 可选 HSV 颜色预过滤
  -> ConnectedComponentsWithStats
  -> 单个连通域 mask
  -> FindContours(CComp)
  -> 计算形状与灰度特征
  -> 过滤
  -> 输出 Blobs / BlobCount / BlobFeatures
```

仓库里虽然还保留了 legacy 分支，但学习和使用当前系统时，应以这条主路径为准。

---

## 10. 参数说明

### 10.1 输入输出端口怎么理解

| 端口 | 作用 |
|------|------|
| `Image` 输入 | 通常是已经分割过、接近前景/背景清晰的图像 |
| `SourceImage` 输入 | 可选原图，用于灰度统计和结果绘制 |
| `Blobs` 输出 | blob 特征列表 |
| `BlobFeatures` 输出 | 打开详细特征后得到更完整的特征集 |
| `BlobCount` 输出 | 过滤后的 blob 数量 |

### 10.2 关键参数怎么调

| 参数 | 默认值 | 作用 |
|------|------|------|
| `MinArea` | `100` | 过滤掉太小的噪点 |
| `MaxArea` | `100000` | 防止整块背景被当成目标 |
| `Color` | `White` | 定义前景极性，必要时反转二值图 |
| `MinCircularity` | `0.0` | 保留更接近圆形的目标 |
| `MinConvexity` | `0.0` | 过滤有明显凹陷的区域 |
| `MinInertiaRatio` | `0.0` | 过滤过细长区域 |
| `MinRectangularity` | `0.0` | 保留更像矩形的区域 |
| `MinEccentricity` | `0.0` | 倾向保留细长目标 |
| `OutputDetailedFeatures` | `false` | 是否输出更全特征 |
| `FeatureFilter` | `""` | 用表达式做二次筛选 |

### 10.3 当前实现里最值得记住的几点

- 带孔目标默认仍算 **1 个 blob**
- 孔洞会通过 `HoleCount` 和 `EulerNumber` 反映
- `Rectangularity` 基于轴对齐外接矩形，所以旋转矩形会被低估

---

## 11. 推荐使用链路与调参建议

### 11.1 常见链路

```text
Gray / Color Filter
  -> Threshold / AdaptiveThreshold
  -> Morphology
  -> BlobDetection
```

### 11.2 调参建议

- 面积阈值永远是第一层筛选
- 如果目标形状比较稳定，再叠加圆度、凸度、矩形度
- 如果输入是原始灰度图，不要直接把它想象成"Blob 算子会自动帮你分割好"

---

## 12. 这个算子的边界

### 12.1 它严重依赖前景分割质量

当前主路径的阈值策略比较保守，如果上游没有先把前景/背景整理清楚，很容易出现整图误检。

### 12.2 它是区域分析，不是子像素测量

做区域计数、形状筛选很合适；做高精度边缘定位和尺寸测量，就要考虑 [CaliperTool](./15-CaliperTool-技术笔记.md) 或 [SubpixelEdgeDetection](./07-SubpixelEdgeDetection-技术笔记.md)。

### 12.3 连通定义会影响"算几个目标"

如果两个缺陷在二值图里已经粘到一起，连通域就会把它们算成一个。

---

## 13. 失败案例与常见误区

### 案例 1：原图直接送入，整张图成了一个大 blob

这通常不是 blob 特征公式的问题，而是前景分割没做好。

### 案例 2：两个近邻缺陷被算成一个

根因通常是二值图里已经连通。

### 常见误区

- 误区一：BlobDetection 就是轮廓检测  
  它更偏向"连通域 + 区域特征"，轮廓只是其中一环。
- 误区二：带孔目标会被拆成多个 blob  
  当前实现不是这样。
- 误区三：所有特征都要用  
  实际上只需选择对当前任务最敏感的特征即可。

---

## 14. 专业来源与延伸阅读

- ClearVision 本地实现: `../../Acme.Product/src/Acme.Product.Infrastructure/Operators/BlobDetectionOperator.cs`
- ClearVision 本地资料: `../算子手册.md`、`../算子名片/BlobAnalysis.md`
- OpenCV Documentation: connected components, contours, moments
- Gonzalez & Woods, *Digital Image Processing*, connected component labeling and morphology
- Szeliski, *Computer Vision: Algorithms and Applications*, region analysis sections

---

## 15. 一句话总结

`BlobDetection` 更像一个"基于连通域的区域分析器"：先把前景区域分出来，再用形状和灰度特征做筛选，而不是直接完成语义级识别。

---

## 推荐下一步阅读

学完这篇后，建议读 [12-FindContours-技术笔记](./12-FindContours-技术笔记.md)，从“区域统计”进一步走到“边界与形状描述”。
