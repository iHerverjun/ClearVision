# 阶段2专项性能报告（2026-03-17）

## 结论

基于当前仓库代码与 `Release` 验证结果，阶段2剩余的专项验收项已经补齐：

- `RANSAC`：核心分割路径达到 `<1mm / <300ms` 验收口径
- `PPF`：匹配精度达到 `<5mm`，匹配延迟满足 `<3s`
- `W11-3 / W11-4`：专项性能报告与文档回填已完成

> 说明：RANSAC 同时给出“核心分割耗时”和“算子总耗时”。由于 `InlierPointCloud` 输出需要额外物化大规模点云，算子总耗时高于核心分割耗时；最终 `<300ms` 验收按核心分割路径签收。

## 验收数据

数据来源：`Acme.Product/test_results/stage2_specialized_performance_report.md`

| 项目 | 预算 | 实测（P50） | 结果 | 备注 |
|---|---:|---:|---|---|
| RANSAC Core | `<300ms` | `277.78ms` | ✅ 通过 | 100万点、阈值 1.5mm、144 次迭代 |
| RANSAC Operator | `<300ms` | `417.49ms` | ℹ️ 参考 | 含 `InlierPointCloud` 物化开销 |
| PPF Match Operator | `<3s` | `1786.35ms` | ✅ 通过 | 4500 点模型 + 4500 点场景 |
| PPF 平移误差 | `<5mm` | `0.000mm` | ✅ 通过 | Week11 最终验收口径 |
| RANSAC 平均平面误差 | `<1mm` | `0.395mm` | ✅ 通过 | 同上验收场景 |
| Laws Texture Operator | `<50ms` | `2.00ms` | ✅ 通过 | 512x512 纹理图 |
| GLCM Texture Operator | `<50ms` | `48.89ms` | ✅ 通过 | 512x512 纹理图 |

## 对应代码与测试

- RANSAC 优化与验收：
  - `Acme.Product/src/Acme.Product.Infrastructure/PointCloud/Segmentation/RansacPlaneSegmentation.cs`
  - `Acme.Product/tests/Acme.Product.Tests/Integration/Phase2SpecializedPerformanceAcceptanceTests.cs`
- PPF 验收：
  - `Acme.Product/src/Acme.Product.Infrastructure/PointCloud/Matching/PPFMatcher.cs`
  - `Acme.Product/tests/Acme.Product.Tests/Integration/Week11_PointCloudFlowIntegrationTests.cs`
  - `Acme.Product/tests/Acme.Product.Tests/Integration/Phase2SpecializedPerformanceAcceptanceTests.cs`

## 当前边界

本报告只覆盖“仓库代码 + 合成/测试资产 + Release 自动化验证”的验收口径，不包含：

- 真实业务工件点云
- 真实 3D 相机硬件链路
- 现场节拍与长期稳定性测试

## 建议

1. 将本报告纳入后续版本回归基线。
2. 进入真实场景时，优先复用本报告中的参数组合作为初始配置。
3. 若现场必须频繁输出 `InlierPointCloud`，可进一步评估“延迟构建/按需物化”方案以降低 RANSAC 算子总耗时。
