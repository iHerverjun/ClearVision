---
title: "ClearVision 对标 Halcon 算子深化路线图（Vibe Coding版）"
doc_type: "plan"
status: "closed"
topic: "对标Halcon"
created: "2026-03-16"
updated: "2026-03-20"
---
# ClearVision 对标 Halcon 算子深化路线图（Vibe Coding版）

> **文档编号**: roadmap-halcon-deep-dive
> **版本**: V2.0-Vibe
> **创建日期**: 2026-03-16
> **目标**: AI辅助编程友好型深化路线，小步快跑，快速验证

---

## 一、Vibe Coding原则

### 1.1 核心原则

| 原则                 | 说明                       | 实践方式                  |
| -------------------- | -------------------------- | ------------------------- |
| **原子化任务** | 每个任务可独立完成，<2小时 | 按算子/功能拆分为最小单元 |
| **即时验证**   | 每步都有自动化测试         | 单元测试先行，失败即停    |
| **Prompt就绪** | 每个任务附带AI生成提示     | 复制粘贴即可生成代码框架  |
| **迭代增量**   | 从MVP到完善，逐步增强      | 先跑通，再优化            |
| **可复制复现** | 测试数据和预期结果明确     | 提供标准测试图和数值基准  |

### 1.2 文档使用方式

```
用户/AI助手使用流程：
1. 选择当前阶段任务
2. 复制任务对应的Prompt模板
3. AI生成代码框架
4. 填入测试数据验证
5. 通过→提交；失败→调整Prompt重试
```

---

## 二、性能要求与质量标准

### 2.1 性能预算表

为确保工业实时性，所有算子必须满足以下性能要求：

| 算子类别           | 目标延迟     | 内存限制 | 备注                     |
| ------------------ | ------------ | -------- | ------------------------ |
| **预处理**   | <10ms        | 100MB    | 滤波、增强、变换         |
| **测量**     | <50ms        | 50MB     | 卡尺、圆测量、轮廓测量   |
| **匹配定位** | <200ms       | 200MB    | 模板匹配、形状匹配       |
| **3D处理**   | <500ms       | 500MB    | 点云滤波、分割、配准     |
| **深度学习** | <100ms (GPU) | 2GB      | YOLO推理、分割、异常检测 |
| **通信**     | <20ms        | 10MB     | Modbus、TCP、串口        |

### 2.2 质量标准

| 指标                     | 要求               | 验证方式              |
| ------------------------ | ------------------ | --------------------- |
| **单元测试覆盖率** | >80%               | 自动化测试报告        |
| **精度验证**       | 与Halcon误差<5%    | 标准测试图对比        |
| **鲁棒性**         | 噪声SNR=20dB下稳定 | 加噪测试              |
| **内存泄漏**       | 1000次运行无泄漏   | Valgrind/内存分析工具 |
| **API稳定性**      | 向后兼容           | 版本回归测试          |

### 2.3 验收标准

每个算子完成后必须通过：

- ✅ 编译无警告
- ✅ 单元测试全部通过
- ✅ 性能满足预算要求
- ✅ 代码审查通过
- ✅ 文档完整（算法说明+使用示例）

---

## 三、能力差距全景分析

### 3.1 差距矩阵（AI优化优先级）

| 能力                 | 差距       | AI实现难度 | 优先级 | 推荐实现顺序                              |
| -------------------- | ---------- | ---------- | ------ | ----------------------------------------- |
| **亚像素卡尺** | ⭐⭐⭐⭐⭐ | ⭐⭐☆☆☆ | P0     | **第1周** ✅ 算法成熟，OpenCV有参考 |
| **Blob特征**   | ⭐⭐⭐☆☆ | ⭐☆☆☆☆ | P0     | **第1周** ✅ 纯计算，无依赖         |
| **ROI跟踪**    | ⭐⭐⭐☆☆ | ⭐⭐☆☆☆ | P1     | **第2周** ✅ 坐标变换逻辑清晰       |
| **形状匹配**   | ⭐⭐⭐⭐⭐ | ⭐⭐⭐☆☆ | P1     | **第3-4周** ⚠️ 需金字塔+梯度计算  |
| **纹理分析**   | ⭐⭐⭐⭐☆ | ⭐⭐☆☆☆ | P2     | **第6周** ✅ Laws滤波有现成公式     |
| **3D点云**     | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ | P2     | **第7-8周** ⚠️ 需PCL/Open3D参考   |
| **深度学习**   | ⭐⭐⭐☆☆ | ⭐⭐☆☆☆ | P3     | **第10周** ✅ ONNX模型集成          |

### 3.2 AI友好任务特征

✅ **适合AI实现**：

- 算法原理明确（有论文/教程）
- OpenCV/PCL有参考实现
- 输入输出数据格式清晰
- 可单元测试验证

⚠️ **需要人工介入**：

- 性能优化（需Profiling）
- 硬件SDK对接（需设备）
- 复杂的UI交互设计

---

## 四、第0周：基础设施搭建（新增）

> **重要性**：没有基准测试基础设施就无法衡量进度和验证性能要求

### 4.1 为什么需要第0周？

在开始算法开发前，必须先建立：

- 性能分析工具：确保每个算子满足延迟预算
- 内存监控：防止内存泄漏导致生产环境崩溃
- 测试数据生成：在没有真实硬件时也能验证算法
- 基准测试套件：量化对比新旧实现的性能

### 4.2 W0任务清单

#### W0-1：性能分析工具（2小时）

```markdown
【任务】实现算子性能分析工具

【要求】
1. 封装Stopwatch计时器
2. 记录每个算子的执行时间
3. 生成性能报告（CSV/JSON）
4. 支持多次运行取平均值

【输出】
- PerformanceProfiler.cs
- 使用示例：
  ```csharp
  using (var profiler = new PerformanceProfiler("CaliperTool"))
  {
      // 算子执行代码
  }
  // 自动记录到性能日志
```

【验证】

- 测试100次运行，统计平均值、最大值、最小值、标准差

```

#### W0-2：内存泄漏检测设置（1小时）

```markdown
【任务】配置内存泄漏检测工具

【要求】
1. 集成.NET内存分析工具（dotMemory或JetBrains工具）
2. 编写内存泄漏测试脚本
3. 设置CI管道自动检测

【验证标准】
- 1000次算子调用后，内存增长<10MB
```

#### W0-3：合成测试数据生成器（3小时）

```markdown
【任务】实现标准测试图生成工具

【要求】
1. 生成ISO 12233斜边图（亚像素测试）
2. 生成几何形状图（圆、矩形、三角形）
3. 生成带噪声版本（高斯噪声、椒盐噪声）
4. 生成3D点云（平面、球体、圆柱体）

【输出】
- TestDataGenerator.cs
- 标准测试图库（保存到 tests/TestData/）

【参考】
- OpenCV: cv2.addWeighted, cv2.GaussianNoise
- 点云生成：使用参数方程生成几何体
```

#### W0-4：现有算子基准测试套件（2小时）

```markdown
【任务】为现有118个算子建立性能基线

【要求】
1. 遍历所有算子
2. 使用标准测试图运行
3. 记录当前性能数据
4. 生成基准报告

【输出】
- baseline_performance.json（当前性能数据）
- 用于后续对比新实现是否有性能回归
```

### 4.3 W0验收标准

- ✅ 性能分析工具可用，能记录任意算子的执行时间
- ✅ 内存泄漏检测配置完成
- ✅ 至少生成10种标准测试图
- ✅ 现有算子性能基线建立

---

## 五、核心任务详解（含AI Prompt模板）

### 5.1 亚像素卡尺（P0，第1周）

#### 为什么先做这个？

- 算法成熟，Halcon原理公开
- OpenCV亚像素角点检测可参考
- 有明确精度指标可验证

#### AI Prompt模板

```markdown
【任务】实现亚像素边缘检测卡尺工具

【上下文】
- 基于OpenCvSharp4
- 已有Mat图像输入
- 需要实现灰度重心法和Zernike矩法

【要求】
1. 实现灰度重心法（快，精度0.1-0.3像素）
2. 实现Zernike矩法（精，精度0.01-0.05像素）
3. 支持边缘对测量（两个边缘间的距离）
4. 包含单元测试，验证精度

【输入输出规格】
- 输入：Mat roiLine（1xN的灰度线条）
- 输出：float subPixelPosition（亚像素位置）

【参考公式】
灰度重心法：
  position = Σ(i * gray[i]) / Σ(gray[i])

Zernike矩法：
  参考Ghosal & Mehrotra 1993论文
  计算Z00, Z11, Z20矩，推导边缘位置

【测试数据】
- 测试图：ISO 12233斜边测试图
- 期望精度：<0.05像素

【生成代码结构】
- SubPixelEdgeDetector.cs（检测器类）
- AdvancedCaliperOperator.cs（算子包装）
- SubPixelEdgeDetectorTests.cs（单元测试）
```

#### 验证Checklist

- [x] 斜边测试精度≤0.05像素
- [x] 1000次重复测量std<0.02像素
- [x] 与OpenCV cornerSubPix结果对比，误差<1%

---

### 3.2 Blob特征扩展（P0，第1周，与上面并行）

#### AI Prompt模板

```markdown
【任务】扩展Blob分析算子，增加工业级特征

【上下文】
- 现有基础BlobAnalysis算子
- 使用OpenCvSharp的ConnectedComponents

【要求】
1. 基于现有连通域结果，计算以下特征：
   - 圆度 Circularity = 4π*Area/Perimeter²
   - 凸性 Convexity = Area/ConvexHullArea
   - 矩形度 Rectangularity = Area/(Width*Height)
   - 离心率 Eccentricity
   - 欧拉数 EulerNumber
2. 支持灰度特征（需要原图）：MeanGray, GrayDeviation
3. 支持FeatureFilter表达式筛选

【输入】
- Mat binaryImage（二值图）
- Mat sourceImage（原图，可选）
- string featureFilter（筛选表达式，如"Area>100 AND Circularity>0.8"）

【输出】
- BlobFeature[] features（特征数组）

【参考】
- OpenCV: cv2.fitEllipse, cv2.convexHull
- Halcon: region_features文档

【测试数据】
- 圆形：圆度>0.99
- 矩形：矩形度>0.95，圆度<0.8
- 带孔洞区域：欧拉数=1-孔洞数
```

---

### 3.3 鲁棒形状匹配（P1，第3-4周）

#### AI Prompt模板

```markdown
【任务】实现基于边缘梯度的形状匹配（Halcon形状匹配简化版）

【核心算法】
1. 模板创建：
   - 提取模板边缘点（Canny/Sobel）
   - 计算每个边缘点的梯度方向
   - 构建金字塔（3-4层）

2. 搜索匹配：
   - 最粗层快速筛选候选位置
   - 逐层精修
   - 原始层最小二乘精修

3. 匹配评分：
   - 基于梯度方向差异（非灰度值）
   - score = Σ(1 - |Δangle|/π) / N

【要求】
- 支持旋转（-180~+180度）
- 支持缩放（0.8-1.2倍）
- 亚像素定位精度
- 部分遮挡鲁棒性（30%遮挡仍可匹配）

【类设计】
```csharp
class ShapeMatcher {
    void CreateTemplate(Mat template, int pyramidLevels);
    List<MatchResult> Search(Mat searchImage, double minScore);
}

struct MatchResult {
    Point2f Position;  // 亚像素
    float Angle;
    float Scale;
    float Score;
}
```

【参考实现】

- OpenCV shape module
- OpenCV matchTemplate（参考金字塔加速思路）
- Halcon Shape-Based Matching白皮书（原理）

【测试标准】

- 光照变化±50%：匹配分数>0.7
- 30%遮挡：匹配分数>0.6
- 精度：<0.1像素

```

---

### 3.4 3D点云基础（P2，第7-8周）

#### AI Prompt模板

```markdown
【任务】实现3D点云基础数据结构和滤波算子

【依赖】
- 使用Open3D.NET或自研（参考PCL）
- 先实现基础数据结构，再集成相机SDK

【要求】
1. PointCloud数据类：
   - Points: Nx3 float (x,y,z)
   - Colors: Nx3 byte (可选)
   - Normals: Nx3 float (可选)

2. 滤波算子：
   - 统计滤波（移除离群点）
   - 体素下采样
   - 直通滤波（按坐标范围裁剪）

3. 分割算子：
   - RANSAC平面分割
   - 欧氏聚类

【参考】
- PCL: StatisticalOutlierRemoval, VoxelGrid
- Open3D: point_cloud.py

【测试数据】
- 使用公开数据集：Stanford 3D Scanning Repository
- 或使用合成数据（立方体、球体、平面）

【注意】
- 先实现文件I/O（PCD/PLY），再对接相机
- 点云可视化可用Open3D或自研简易渲染
```

---

## 四、Vibe Coding迭代计划

### 4.1 迭代节奏

```
每日节奏：
├── 选择1-2个原子任务（<2小时）
├── AI生成代码框架
├── 填入测试数据
├── 运行测试
├── ✅通过 → Git提交 → 下一个任务
└── ❌失败 → 调整Prompt → 重试

每周节奏：
├── 周一：规划本周3-5个任务
├── 每日：迭代开发
├── 周五：集成测试，阶段验收
└── 周末：复盘，调整下周计划
```

### 4.2 Prompt工程最佳实践

#### 好的Prompt特征

✅ **好的示例**（Blob特征）：

```markdown
【任务】计算Blob圆度
【公式】Circularity = 4 * PI * Area / (Perimeter * Perimeter)
【输入】Mat binaryImage, int labelId
【输出】double circularity
【参考】OpenCV: contourArea, arcLength
【测试】正圆→0.99+，正方形→0.785
```

❌ **不好的示例**：

```markdown
实现一个Blob分析算子，要专业一点，像Halcon那样。
```

#### Prompt模板库

每个任务类别都有标准Prompt模板，复制即用：

- `[Template] 图像滤波算子`
- `[Template] 特征测量算子`
- `[Template] 几何变换算子`
- `[Template] 点云处理算子`

---

## 五、分阶段里程碑

### 阶段1：基础补强（第1-4周）

**目标**：核心测量能力达到工业可用

| 周次 | 任务                        | 验证标准    | Prompt就绪 |
| ---- | --------------------------- | ----------- | ---------- |
| W1   | 亚像素卡尺 + Blob特征       | 精度<0.05px | ✅         |
| W2   | ROI自动跟踪机制             | 延迟<1ms    | ✅         |
| W3   | 形状匹配MVP（基础版）       | 无遮挡匹配  | ✅         |
| W4   | 形状匹配增强（金字塔+遮挡） | 30%遮挡OK   | ✅         |

### 阶段2：专业深化（第5-8周）

**目标**：3D+纹理+颜色，形成专业工具链

| 周次  | 任务                            | 验证标准           | Prompt就绪 |
| ----- | ------------------------------- | ------------------ | ---------- |
| W5-W6 | 3D点云I/O + 滤波                | 加载PCD<1s         | ✅         |
| W7    | 点云分割 + 表面匹配MVP          | 平面分割误差<1mm   | ⚠️       |
| W8    | 纹理分析（Laws/GLCM） + CIE颜色 | 纹理分类准确率>90% | ✅         |

### 阶段3：生态扩展（第9-12周）

**目标**：AI+3D融合，差异化能力

| 周次   | 任务                          | 验证标准       | Prompt就绪 |
| ------ | ----------------------------- | -------------- | ---------- |
| W9-W10 | 深度学习算子（分割/异常检测） | mIoU>0.85      | ✅         |
| W11    | 手眼标定向导                  | 标定误差<0.5mm | ⚠️       |
| W12    | GPU加速 + 集成优化            | 关键算子5x加速 | ⚠️       |

---

## 六、测试数据集（预置）

### 6.1 2D测试图

| 名称                           | 用途           | 获取方式            |
| ------------------------------ | -------------- | ------------------- |
| `iso12233_slant_edge.png`    | 亚像素精度测试 | ISO标准图，网络下载 |
| `circle_square_triangle.png` | Blob特征测试   | 程序生成            |
| `template_matching_test.png` | 形状匹配测试   | 工业样本或合成      |
| `texture_brochette.png`      | 纹理分析测试   | 公开纹理数据集      |

### 6.2 3D测试数据

| 名称                   | 用途         | 获取方式     |
| ---------------------- | ------------ | ------------ |
| `plane_100k.pcd`     | 平面分割测试 | 程序生成     |
| `bunny.pcd`          | 匹配测试     | Stanford兔子 |
| `scene_with_box.ply` | 场景测试     | 合成或扫描   |

### 6.3 测试数据生成脚本

```csharp
// 生成标准测试图
public static class TestDataGenerator
{
    // 生成斜边图（用于亚像素测试）
    public static Mat GenerateSlantEdge(int width, int height, double angle)
    {
        // AI生成实现...
    }
  
    // 生成带噪声的几何形状
    public static Mat GenerateShapeWithNoise(string shape, double noiseLevel)
    {
        // AI生成实现...
    }
}
```

---

## 七、AI编程Checklist

### 每个任务的检查项

```markdown
□ 任务理解
  □ 输入输出数据类型明确
  □ 算法原理/公式清晰
  □ 有参考实现（OpenCV/PCL/Halcon原理）

□ 代码生成
  □ Prompt包含完整上下文
  □ 生成代码可直接编译
  □ 类/方法命名符合项目规范

□ 测试验证
  □ 单元测试用例≥3个
  □ 有预期结果数值
  □ 测试通过/失败标准明确

□ 集成
  □ 符合现有算子基类规范
  □ 元数据（端口/参数）完整
  □ 文档注释完整
```

---

## 十、风险缓解策略

### 10.1 技术风险识别

#### 🔴 高风险项

**1. 形状匹配性能瓶颈**

- **风险**：360度旋转搜索 × 多尺度 × 金字塔层级 = 可能超过1秒
- **影响**：无法满足实时检测要求
- **缓解措施**：
  - 实现角度步长自适应（粗层大步长，细层小步长）
  - 增加早期终止机制（分数过低直接跳过）
  - W12考虑GPU加速（CUDA/OpenCL）
  - 限制搜索区域（基于先验知识）

**2. 点云内存消耗**

- **风险**：100万点 × (xyz + rgb + normal) = 48MB/帧，多相机场景可能OOM
- **影响**：系统崩溃或性能严重下降
- **缓解措施**：
  - 实现流式处理架构（分块加载）
  - 增加内存池管理
  - 设置点云大小上限（超过则自动下采样）
  - 监控内存使用，超过阈值告警

**3. 数值稳定性问题**

- **风险**：Zernike矩、RANSAC、手眼标定对数值精度敏感
- **影响**：结果不稳定、标定失败
- **缓解措施**：
  - 使用double而非float进行中间计算
  - 增加条件数检查（矩阵求逆前）
  - 实现鲁棒估计（RANSAC、M-estimator）
  - 输入数据归一化

#### ⚠️ 中风险项

**4. 并发与线程安全**

- **风险**：ONNX Runtime会话、点云处理可能有共享状态
- **影响**：多线程环境下崩溃或结果错误
- **缓解措施**：
  - 明确标注线程安全性（文档注释）
  - 使用线程本地存储（ThreadLocal）
  - 关键区域加锁（lock/Mutex）
  - 编写并发测试用例

**5. 模型部署一致性**

- **风险**：ONNX模型训练时的预处理与部署时不一致
- **影响**：推理精度下降
- **缓解措施**：
  - W9-0实现模型验证管道
  - 记录预处理步骤（均值、标准差、尺寸）
  - 提供模型测试工具（对比训练框架输出）

**6. 硬件依赖延迟**

- **风险**：3D相机SDK集成推迟，无法验证点云算子
- **影响**：阶段2进度受阻
- **缓解措施**：
  - W5-0优先实现合成点云生成器
  - 使用公开数据集（Stanford、ModelNet）
  - 提前与硬件供应商沟通SDK接口

### 10.2 组织风险

**7. 单线程开发瓶颈**

- **风险**：假设单人顺序开发，无法并行
- **缓解措施**：
  - 任务原子化，便于多人协作
  - 明确模块接口，减少依赖
  - 使用Git分支策略（feature branch）

**8. 技术债务累积**

- **风险**："先跑通，再优化"可能导致性能问题遗留
- **缓解措施**：
  - 每周五代码审查
  - W0建立性能基线，持续监控
  - 预留20%时间用于重构

### 10.3 风险监控机制

| 风险项     | 监控指标       | 告警阈值            | 检查频率 |
| ---------- | -------------- | ------------------- | -------- |
| 性能回归   | 算子执行时间   | 超过基线20%         | 每次提交 |
| 内存泄漏   | 进程内存增长   | 1000次运行增长>50MB | 每日     |
| 测试覆盖率 | 单元测试覆盖率 | <75%                | 每周     |
| 代码质量   | 静态分析警告数 | >10个               | 每次提交 |

---

## 十一、缺失能力路线图

> **说明**：以下是当前12周计划未覆盖但Halcon具备的关键能力，建议在第4阶段（第13-24周）补充

### 11.1 高优先级缺失能力（P0级）

#### 计量与标定

**1. 镜头畸变校正增强**

- **当前状态**：基础Undistort已有，但缺少高级功能
- **缺失功能**：
  - 鱼眼镜头校正（Fisheye模型）
  - 畸变参数优化工具
  - 实时去畸变（查找表加速）
- **建议时间**：W13-W14（4小时）
- **Halcon对标**：`change_radial_distortion_cam_par`

**2. 多相机标定**

- **当前状态**：仅支持单相机标定
- **缺失功能**：
  - 立体相机标定（双目、三目）
  - 相机间外参标定
  - 标定质量评估（重投影误差可视化）
- **建议时间**：W15-W16（8小时）
- **Halcon对标**：`binocular_calibration`

**3. 像素到世界坐标映射**

- **当前状态**：缺失
- **缺失功能**：
  - 基于标定的像素→世界坐标转换
  - 世界坐标→像素反向映射
  - 支持不同坐标系（机器人、工件）
- **建议时间**：W17（3小时）
- **Halcon对标**：`image_points_to_world_plane`

#### 高级匹配

**4. 可变形匹配**

- **当前状态**：仅支持刚性匹配
- **缺失功能**：
  - 局部可变形模板匹配
  - 适用于柔性零件、布料、橡胶
- **建议时间**：W18-W19（10小时）
- **Halcon对标**：`find_local_deformable_model`

**5. 透视匹配**

- **当前状态**：仅支持2D平面匹配
- **缺失功能**：
  - 3D透视变换下的匹配
  - 适用于非平面物体
- **建议时间**：W20（6小时）
- **Halcon对标**：`find_planar_uncalib_deformable_model`

**6. 部分遮挡处理增强**

- **当前状态**：W4-2有基础实现
- **缺失功能**：
  - 遮挡区域自动检测
  - 遮挡率估计
  - 多目标遮挡场景
- **建议时间**：W21（4小时）

#### 测量算子

**7. 圆/弧拟合鲁棒版**

- **当前状态**：基础CircleMeasurement已有
- **缺失功能**：
  - RANSAC离群值剔除
  - 椭圆拟合（非完美圆）
  - 弧段拟合（非完整圆）
- **建议时间**：W22（3小时）
- **Halcon对标**：`fit_circle_contour_xld`

**8. 最小外接几何体**

- **当前状态**：缺失
- **缺失功能**：
  - 最小外接圆（Minimum Enclosing Circle）
  - 最小外接矩形（Minimum Area Rectangle）
  - 最小外接椭圆
- **建议时间**：W23（2小时）
- **Halcon对标**：`smallest_circle`, `smallest_rectangle2`

**9. 距离变换**

- **当前状态**：缺失
- **缺失功能**：
  - 欧氏距离变换
  - 用于间隙检查、安全距离计算
- **建议时间**：W24（2小时）
- **Halcon对标**：`distance_transform`

### 11.2 中优先级缺失能力（P1级）

#### 区域处理

**10. 区域形态学操作**

- **缺失功能**：
  - 区域腐蚀/膨胀（非像素级）
  - 区域开闭运算
  - 区域骨架提取
- **建议时间**：W25-W26（6小时）
- **Halcon对标**：`erosion_region`, `skeleton`

**11. 区域布尔运算**

- **缺失功能**：
  - 区域并集/交集/差集/异或
  - 区域连通性分析
- **建议时间**：W27（3小时）
- **Halcon对标**：`union2`, `intersection`, `difference`

#### 1D测量

**12. 任意路径1D边缘检测**

- **当前状态**：卡尺仅支持矩形ROI
- **缺失功能**：
  - 沿曲线路径的边缘检测
  - 圆弧卡尺、样条卡尺
- **建议时间**：W28（4小时）
- **Halcon对标**：`measure_pos` with arbitrary path

**13. 轮廓峰谷检测**

- **缺失功能**：
  - 1D信号峰值检测
  - 用于表面轮廓分析
- **建议时间**：W29（3小时）
- **Halcon对标**：`local_max_contours_xld`

**14. 傅里叶频谱分析**

- **缺失功能**：
  - 1D FFT分析
  - 周期性缺陷检测
- **建议时间**：W30（4小时）
- **Halcon对标**：`fft_generic`

### 11.3 低优先级缺失能力（P2级）

#### 颜色与光度

**15. 多光源融合**

- **缺失功能**：
  - 不同光照条件下的图像融合
  - 用于镜面反射物体检测
- **建议时间**：W31（5小时）

**16. 光度立体**

- **缺失功能**：
  - 从多光源图像重建3D表面
- **建议时间**：W32-W33（8小时）

**17. 颜色恒常性**

- **缺失功能**：
  - 光照不变的颜色匹配
- **建议时间**：W34（4小时）

### 11.4 缺失能力优先级矩阵

| 能力             | 工业需求度 | 实现难度   | 建议周次 | 对标Halcon算子              |
| ---------------- | ---------- | ---------- | -------- | --------------------------- |
| 镜头畸变校正增强 | ⭐⭐⭐⭐⭐ | ⭐⭐☆☆☆ | W13-W14  | change_radial_distortion    |
| 多相机标定       | ⭐⭐⭐⭐⭐ | ⭐⭐⭐☆☆ | W15-W16  | binocular_calibration       |
| 像素世界坐标映射 | ⭐⭐⭐⭐⭐ | ⭐⭐☆☆☆ | W17      | image_points_to_world_plane |
| 可变形匹配       | ⭐⭐⭐⭐☆ | ⭐⭐⭐⭐☆ | W18-W19  | find_local_deformable_model |
| 透视匹配         | ⭐⭐⭐☆☆ | ⭐⭐⭐☆☆ | W20      | find_planar_uncalib         |
| 鲁棒圆拟合       | ⭐⭐⭐⭐☆ | ⭐⭐☆☆☆ | W22      | fit_circle_contour_xld      |
| 最小外接几何体   | ⭐⭐⭐⭐☆ | ⭐☆☆☆☆ | W23      | smallest_circle             |
| 距离变换         | ⭐⭐⭐☆☆ | ⭐☆☆☆☆ | W24      | distance_transform          |
| 区域形态学       | ⭐⭐⭐☆☆ | ⭐⭐☆☆☆ | W25-W26  | erosion_region              |
| 任意路径卡尺     | ⭐⭐⭐⭐☆ | ⭐⭐⭐☆☆ | W28      | measure_pos                 |

---

## 十二、修订时间表

### 12.1 现实16周计划（推荐）

| 阶段               | 周次    | 核心任务     | 新增内容                      |
| ------------------ | ------- | ------------ | ----------------------------- |
| **准备阶段** | W0      | 基础设施搭建 | 性能分析、测试数据生成        |
| **阶段1**    | W1-W5   | 核心测量能力 | +W1-5镜头畸变，+W4-2遮挡处理  |
| **阶段2**    | W6-W11  | 3D+纹理      | +W5-0合成点云，FPFH→PPF简化  |
| **阶段3**    | W12-W16 | AI+标定      | +W9-0模型验证，+W11-3标定验证 |

### 12.2 保持12周的精简方案

如果必须保持12周，建议推迟以下内容到第4阶段：

- ❌ FPFH特征（改为简化的PPF）
- ❌ 手眼标定（推迟到W15-W16）
- ❌ 异常检测（推迟到W18）
- ✅ 保留：核心2D测量 + 基础3D点云 + 深度学习推理

---

## 十三、快速开始指南

### 今天就能开始

**推荐路径**：

1. **第0周先行**：运行W0-3生成测试数据（3小时）
2. **选择第一个任务**：W1-1灰度重心法（已完成✅）或W1-3 Blob特征扩展
3. **复制Prompt**：从第五节复制对应任务的Prompt
4. **AI生成代码**：粘贴到ChatGPT/Claude
5. **填入项目**：复制到Acme.Product.Infrastructure
6. **运行测试**：使用W0生成的测试数据
7. **性能验证**：使用W0-1性能分析工具确认满足预算
8. **提交代码**：Git commit

### 本周目标

> **W0完成**：测试数据生成器可用，性能基线建立
> **W1完成**：亚像素卡尺 + Blob特征扩展，精度达到Halcon 60%水平

---

## 十四、总结

### Vibe Coding核心理念

> **不是"要4个人做6个月"，而是"AI+人，每周迭代，每周有成果"**

### 成功指标

| 时间     | 可演示成果              | 验证方式       |
| -------- | ----------------------- | -------------- |
| 第0周末  | 测试数据生成器+性能基线 | 生成10种测试图 |
| 第1周末  | 亚像素测量精度<0.05px   | 斜边测试图     |
| 第5周末  | 形状匹配鲁棒性OK        | 遮挡测试视频   |
| 第11周末 | 3D点云处理可用          | 点云可视化截图 |
| 第16周末 | AI+3D融合演示           | 端到端检测Demo |

### 文档更新记录

| 版本          | 日期       | 更新内容                                         |
| ------------- | ---------- | ------------------------------------------------ |
| V2.0-Vibe     | 2026-03-16 | 初始版本，Vibe Coding方法论                      |
| V2.1-Enhanced | 2026-03-16 | 增加性能要求、W0基础设施、风险缓解、缺失能力分析 |

---

*本文档持续更新，每个任务完成后补充实际Prompt和踩坑记录*


